# Racha de entrenamiento Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Contador de racha (días consecutivos con al menos un entrenamiento) visible en `index.html`, alimentado por los 5 modos de entrenamiento de la app (Casa, Gimnasio, Ejercicio rápido, los 3 timers de cardio, Correr/Caminar).

**Architecture:** `streak.js` es un script clásico nuevo (sin build, sin módulos) que expone `window.Streak` con lógica pura de cálculo de racha + persistencia en `localStorage` (`qg_activity_log`, un array de fechas `"YYYY-MM-DD"`, una por día con actividad). Cada uno de los 5 puntos de "sesión completada" llama a `Streak.recordActivity()`. Solo `index.html` llama a `Streak.getCurrentStreak()` para mostrar el número.

**Tech Stack:** HTML/JS puro (sin build), Node.js solo para verificación (`node --check`, scripts de assert).

## Global Constraints

- Registrar peso en "Tu progreso" NO cuenta para la racha — solo las 5 actividades de entrenamiento.
- `qg_activity_log`: array de fechas `"YYYY-MM-DD"`, una entrada por día (no duplicar si ya existe la de hoy).
- Racha estricta: un día calendario completo sin actividad la rompe a 0 en el próximo cálculo. Sin gracia.
- Si hoy no hay actividad pero ayer sí, la racha sigue contando desde ayer hacia atrás (no se rompe a medio día).
- `streak.js` no debe referenciar `QuemaSync`, `sync.js`, `WeightLog`, ni DOM — solo lógica pura + `localStorage`.
- **Importante:** `cardio.html` ya tiene su propia función global `computeStreak()` (racha local solo de sesiones de cardio, sin relación con esta feature — no tocar). Para evitar colisión de nombres en el scope global compartido de esa página, la función pura de `streak.js` se llama `computeCurrentStreak`, **no** `computeStreak`.
- Sin sincronización con Supabase en esta ronda — 100% local, igual que Gimnasio y Progreso hoy.
- Spec completo: `docs/superpowers/specs/2026-08-22-racha-design.md`.

---

### Task 1: Crear `streak.js`

**Files:**
- Create: `streak.js`

**Interfaces:**
- Produce (usado por Tasks 2–6): `window.Streak` con:
  ```
  Streak = {
    todayStr(d?) -> "YYYY-MM-DD",
    computeCurrentStreak(log: string[], todayDateStr: string) -> number,  // pura, testeable
    recordActivity() -> void,   // agrega la fecha de hoy a qg_activity_log si no está ya
    getLog() -> string[],
    getCurrentStreak() -> number  // conveniencia: computeCurrentStreak(getLog(), todayStr())
  }
  ```

- [ ] **Step 1: Escribir `streak.js`**

```javascript
/* =====================================================================
   streak.js — racha de entrenamiento compartida: registro de "hoy hubo
   actividad" y cálculo de días consecutivos. Sin conocimiento de DOM ni
   de Supabase — todo local en localStorage. Compartido por
   entrenamientos.html/gimnasio.html (vía plan-engine.js), rapido.html,
   cardio.html y correr.html.
   Ver docs/superpowers/specs/2026-08-22-racha-design.md
   ===================================================================== */

/* ---------- lógica pura (testeable sin red/DOM) ---------- */
function todayStr(d){
  d = d || new Date();
  const y = d.getFullYear();
  const m = String(d.getMonth()+1).padStart(2,'0');
  const day = String(d.getDate()).padStart(2,'0');
  return `${y}-${m}-${day}`;
}

function daysBetween(dateStrA, dateStrB){
  return (new Date(dateStrB+'T00:00:00') - new Date(dateStrA+'T00:00:00')) / 86400000;
}

function computeCurrentStreak(log, todayDateStr){
  if(!log.length) return 0;
  const days = [...new Set(log)].sort().reverse();
  const gap = daysBetween(days[0], todayDateStr);
  if(gap > 1) return 0;
  let streak = 1;
  for(let i=1; i<days.length; i++){
    if(daysBetween(days[i], days[i-1]) === 1) streak++;
    else break;
  }
  return streak;
}
/* ---------- fin lógica pura ---------- */

const KEY_LOG = 'qg_activity_log';

function getLog(){ try{ return JSON.parse(localStorage.getItem(KEY_LOG))||[]; }catch(e){ return []; } }
function saveLog(log){ try{ localStorage.setItem(KEY_LOG, JSON.stringify(log)); }catch(e){} }

function recordActivity(){
  const today = todayStr();
  const log = getLog();
  if(!log.includes(today)){
    log.push(today);
    saveLog(log);
  }
}

function getCurrentStreak(){
  return computeCurrentStreak(getLog(), todayStr());
}

window.Streak = { todayStr, computeCurrentStreak, recordActivity, getLog, getCurrentStreak };
```

- [ ] **Step 2: Verificar sintaxis**

```bash
node --check streak.js
```
Expected: sin salida (exit code 0).

- [ ] **Step 3: Assert-based check de la lógica pura**

```bash
node -e "
const fs=require('fs');
const src=fs.readFileSync('streak.js','utf8');
const start=src.indexOf('/* ---------- lógica pura');
const end=src.indexOf('/* ---------- fin lógica pura ---------- */');
const pureCode=src.slice(start,end);
const test=pureCode+\`
console.assert(todayStr(new Date(2026,0,5))==='2026-01-05', 'todayStr debe formatear con ceros');

console.assert(computeCurrentStreak([], '2026-08-22')===0, 'log vacío debe dar 0');
console.assert(computeCurrentStreak(['2026-08-22'], '2026-08-22')===1, 'solo hoy debe dar 1');
console.assert(computeCurrentStreak(['2026-08-21'], '2026-08-22')===1, 'solo ayer (sin hoy) debe seguir dando 1, no romperse a medio día');
console.assert(computeCurrentStreak(['2026-08-22','2026-08-22'], '2026-08-22')===1, 'fechas duplicadas no deben inflar la racha');

const consec = computeCurrentStreak(['2026-08-20','2026-08-21','2026-08-22'], '2026-08-22');
console.assert(consec===3, '3 días consecutivos terminando hoy debe dar 3, dio '+consec);

const brokenByGap = computeCurrentStreak(['2026-08-18','2026-08-19','2026-08-20'], '2026-08-22');
console.assert(brokenByGap===0, 'hueco de 2+ días antes de hoy debe romper la racha a 0, dio '+brokenByGap);

const stopsAtGap = computeCurrentStreak(['2026-08-15','2026-08-20','2026-08-21','2026-08-22'], '2026-08-22');
console.assert(stopsAtGap===3, 'debe contar solo la racha consecutiva más reciente (3), ignorando el día suelto anterior, dio '+stopsAtGap);

// casos que cruzan un cambio de horario de verano (regresión: daysBetween debe comparar en UTC,
// si alguna vez pierde el sufijo Z estas comparaciones de igualdad estricta se rompen en zonas con DST)
const springForward = computeCurrentStreak(['2026-03-07','2026-03-08','2026-03-09'], '2026-03-09');
console.assert(springForward===3, 'racha de 3 días cruzando el cambio a horario de verano (marzo) debe seguir dando 3, dio '+springForward);
const fallBack = computeCurrentStreak(['2026-11-01'], '2026-11-02');
console.assert(fallBack===1, 'racha de ayer cruzando el fin del horario de verano (noviembre) debe seguir dando 1, dio '+fallBack);

console.log('OK streak pure logic');
\`;
fs.writeFileSync('.streak-check.js', test);
"
node .streak-check.js
rm -f .streak-check.js
```
Expected output: `OK streak pure logic` y ninguna línea `Assertion failed`. Para confirmar que los casos de DST realmente ejercitan una zona horaria con horario de verano (no solo pasan por casualidad en una zona sin DST), correr también con `TZ=America/Tijuana node .streak-check.js` antes de borrar el archivo temporal.

- [ ] **Step 4: Commit**

```bash
git add streak.js
git commit -m "feat(racha): add streak.js — shared activity log and current-streak logic"
```

---

### Task 2: Enganchar Casa y Gimnasio (`plan-engine.js`)

**Files:**
- Modify: `plan-engine.js`
- Modify: `entrenamientos.html`
- Modify: `gimnasio.html`

**Interfaces:**
- Consumes: `window.Streak.recordActivity()` de Task 1.

- [ ] **Step 1: Cargar `streak.js` en `entrenamientos.html`**

Ubicar:
```html
<script src="sync.js"></script>
<script src="plan-engine.js"></script>
```
reemplazar por:
```html
<script src="sync.js"></script>
<script src="streak.js"></script>
<script src="plan-engine.js"></script>
```

- [ ] **Step 2: Cargar `streak.js` en `gimnasio.html`**

Ubicar:
```html
<script src="plan-engine.js"></script>
<script>
```
reemplazar por:
```html
<script src="streak.js"></script>
<script src="plan-engine.js"></script>
<script>
```

- [ ] **Step 3: Enganchar `completeSession()` en `plan-engine.js`**

Ubicar:
```javascript
function completeSession(){
  COMPLETED.add(currentDay);
  saveCompleted();
  document.getElementById('complete-day').textContent = currentDay;
  showScreen('complete');
}
```
reemplazar por:
```javascript
function completeSession(){
  COMPLETED.add(currentDay);
  saveCompleted();
  if(window.Streak) Streak.recordActivity();
  document.getElementById('complete-day').textContent = currentDay;
  showScreen('complete');
}
```

- [ ] **Step 4: Verificar sintaxis**

```bash
node --check plan-engine.js
node -e "
const fs=require('fs');
['entrenamientos.html','gimnasio.html'].forEach(f=>{
  const src=fs.readFileSync(f,'utf8');
  const body=src.slice(src.lastIndexOf('<script>')+8, src.lastIndexOf('</script>'));
  fs.writeFileSync('.syntax-check.js', body);
  require('child_process').execSync('node --check .syntax-check.js');
});
console.log('OK');
"
rm -f .syntax-check.js
```
Expected: `OK`, sin errores.

- [ ] **Step 5: Prueba manual**

Sirve el sitio localmente y, con `localStorage` limpio (o al menos sin `qg_activity_log`):
1. En `entrenamientos.html`, completa un día del plan (marca ✓ en todos los ejercicios de una sesión) hasta llegar a "Día X completo".
2. En devtools, confirma que `localStorage.qg_activity_log` ahora tiene la fecha de hoy.
3. Repite en `gimnasio.html` completando un día — confirma que NO duplica la fecha de hoy (sigue habiendo un solo elemento con la fecha de hoy en el array, aunque se haya completado en dos modos distintos el mismo día).

- [ ] **Step 6: Commit**

```bash
git add plan-engine.js entrenamientos.html gimnasio.html
git commit -m "feat(racha): record activity on completing a Casa/Gimnasio day"
```

---

### Task 3: Enganchar Ejercicio rápido (`rapido.html`)

**Files:**
- Modify: `rapido.html`

**Interfaces:**
- Consumes: `window.Streak.recordActivity()` de Task 1.

- [ ] **Step 1: Cargar `streak.js`**

Ubicar:
```html
    <button class="complete-btn secondary" onclick="goHome()">Volver al inicio</button>
  </div>
</div>

<script>
```
reemplazar por:
```html
    <button class="complete-btn secondary" onclick="goHome()">Volver al inicio</button>
  </div>
</div>

<script src="streak.js"></script>
<script>
```

- [ ] **Step 2: Enganchar `finishSession()`**

Ubicar:
```javascript
function finishSession(){
  stopTimer();
  releaseWakeLock();
  vibrate([300,100,300,100,600]);
  beep(523,0.15,0.3); setTimeout(()=>beep(659,0.15,0.3),160); setTimeout(()=>beep(784,0.35,0.3),330);
  document.getElementById('complete-sub').textContent = `${session.length} ejercicios hechos. Buen trabajo.`;
  showScreen('complete');
}
```
reemplazar por:
```javascript
function finishSession(){
  stopTimer();
  releaseWakeLock();
  vibrate([300,100,300,100,600]);
  beep(523,0.15,0.3); setTimeout(()=>beep(659,0.15,0.3),160); setTimeout(()=>beep(784,0.35,0.3),330);
  if(window.Streak) Streak.recordActivity();
  document.getElementById('complete-sub').textContent = `${session.length} ejercicios hechos. Buen trabajo.`;
  showScreen('complete');
}
```

- [ ] **Step 3: Verificar sintaxis**

```bash
node -e "
const fs=require('fs');
const src=fs.readFileSync('rapido.html','utf8');
const body=src.slice(src.lastIndexOf('<script>')+8, src.lastIndexOf('</script>'));
fs.writeFileSync('.syntax-check.js', body);
"
node --check .syntax-check.js
rm -f .syntax-check.js
```
Expected: sin salida (exit code 0).

- [ ] **Step 4: Prueba manual**

Sirve el sitio localmente, completa una ronda de 10 minutos en `rapido.html` (o adelanta el estado si es muy largo probarlo completo), y confirma en devtools que `qg_activity_log` tiene la fecha de hoy.

- [ ] **Step 5: Commit**

```bash
git add rapido.html
git commit -m "feat(racha): record activity on finishing an Ejercicio rápido session"
```

---

### Task 4: Enganchar los timers de cardio (`cardio.html`)

**Files:**
- Modify: `cardio.html`

**Interfaces:**
- Consumes: `window.Streak.recordActivity()` de Task 1.

**Nota:** `cardio.html` ya tiene su propia función `computeStreak()` (sin relación, racha local solo de sesiones de cardio) — no tocar esa función ni su UI existente (el mensaje "🔥 N días seguidos" en la pantalla de fin de sesión de cardio). Esta tarea solo agrega la llamada a la racha global nueva, en paralelo.

- [ ] **Step 1: Cargar `streak.js`**

Ubicar:
```html
<audio id="silent-audio" loop>
  <source src="data:audio/wav;base64,UklGRiQAAABXQVZFZm10IBAAAAABAAEARKwAAIhYAQACABAAZGF0YQAAAAA=" type="audio/wav">
</audio>

<script>
```
reemplazar por:
```html
<audio id="silent-audio" loop>
  <source src="data:audio/wav;base64,UklGRiQAAABXQVZFZm10IBAAAAABAAEARKwAAIhYAQACABAAZGF0YQAAAAA=" type="audio/wav">
</audio>

<script src="streak.js"></script>
<script>
```

- [ ] **Step 2: Enganchar `recordSession()`**

Ubicar:
```javascript
async function recordSession(){
  sessions.push({d:dayKey(), min:durKey, lvl:levelKey, mac:machineKey,
                 kcal:Math.round(kcal), spr:TOTAL_SPRINTS});
  if(sessions.length>500) sessions=sessions.slice(-500);
  try{ await Store.set(KEY_HIST,sessions); }catch(e){}
  renderHistory();
  const st=computeStreak();
  if(st>1){
    const el=document.getElementById('finish-sub');
    el.textContent=el.textContent+` · 🔥 ${st} días seguidos`;
  }
}
```
reemplazar por:
```javascript
async function recordSession(){
  sessions.push({d:dayKey(), min:durKey, lvl:levelKey, mac:machineKey,
                 kcal:Math.round(kcal), spr:TOTAL_SPRINTS});
  if(sessions.length>500) sessions=sessions.slice(-500);
  try{ await Store.set(KEY_HIST,sessions); }catch(e){}
  renderHistory();
  if(window.Streak) Streak.recordActivity();
  const st=computeStreak();
  if(st>1){
    const el=document.getElementById('finish-sub');
    el.textContent=el.textContent+` · 🔥 ${st} días seguidos`;
  }
}
```

- [ ] **Step 3: Verificar sintaxis**

```bash
node -e "
const fs=require('fs');
const src=fs.readFileSync('cardio.html','utf8');
const body=src.slice(src.lastIndexOf('<script>')+8, src.lastIndexOf('</script>'));
fs.writeFileSync('.syntax-check.js', body);
"
node --check .syntax-check.js
rm -f .syntax-check.js
```
Expected: sin salida (exit code 0).

- [ ] **Step 4: Prueba manual**

Sirve el sitio localmente, termina un timer en `cardio.html` (cualquiera de Elíptica/Caminadora/Escaladora), y confirma en devtools que `qg_activity_log` tiene la fecha de hoy, Y que el mensaje local "🔥 N días seguidos" de cardio (el ya existente, de `computeStreak()`) se sigue viendo igual que antes en la pantalla de fin de sesión — sin cambios de comportamiento ahí.

- [ ] **Step 5: Commit**

```bash
git add cardio.html
git commit -m "feat(racha): record activity on finishing a cardio timer session"
```

---

### Task 5: Enganchar Correr/Caminar (`correr.html`)

**Files:**
- Modify: `correr.html`

**Interfaces:**
- Consumes: `window.Streak.recordActivity()` de Task 1.

- [ ] **Step 1: Cargar `streak.js`**

Ubicar:
```html
<script src="sync.js"></script>
<script src="weight-log.js"></script>
<script>
```
reemplazar por:
```html
<script src="sync.js"></script>
<script src="weight-log.js"></script>
<script src="streak.js"></script>
<script>
```

- [ ] **Step 2: Enganchar el guardado de sesión terminada**

Ubicar:
```javascript
  const entry = { date: Date.now(), mode, distanceM, elapsedS, steps, kcal };
  saveHistoryEntry(entry);
  if(window.QuemaSync) QuemaSync.pushRun(entry);
```
reemplazar por:
```javascript
  const entry = { date: Date.now(), mode, distanceM, elapsedS, steps, kcal };
  saveHistoryEntry(entry);
  if(window.Streak) Streak.recordActivity();
  if(window.QuemaSync) QuemaSync.pushRun(entry);
```

- [ ] **Step 3: Verificar sintaxis**

```bash
node -e "
const fs=require('fs');
const src=fs.readFileSync('correr.html','utf8');
const body=src.slice(src.lastIndexOf('<script>')+8, src.lastIndexOf('</script>'));
fs.writeFileSync('.syntax-check.js', body);
"
node --check .syntax-check.js
rm -f .syntax-check.js
```
Expected: sin salida (exit code 0).

- [ ] **Step 4: Prueba manual**

Sirve el sitio localmente, termina una sesión corta de Correr/Caminar en `correr.html` (o simula el fin de sesión), y confirma en devtools que `qg_activity_log` tiene la fecha de hoy y que el flujo de resumen/kcal/historial de Correr sigue funcionando igual que antes.

- [ ] **Step 5: Commit**

```bash
git add correr.html
git commit -m "feat(racha): record activity on finishing a Correr/Caminar session"
```

---

### Task 6: Mostrar la racha en `index.html`

**Files:**
- Modify: `index.html`

**Interfaces:**
- Consumes: `window.Streak.getCurrentStreak()` de Task 1.

- [ ] **Step 1: CSS del contador**

Ubicar:
```css
.hero { text-align:center; margin-bottom:26px; }
.hero-title { font-size:26px; font-weight:800; letter-spacing:-.01em; }
.hero-sub { font-size:12.5px; color:var(--muted); margin-top:6px; }
```
reemplazar por:
```css
.hero { text-align:center; margin-bottom:26px; }
.hero-title { font-size:26px; font-weight:800; letter-spacing:-.01em; }
.hero-sub { font-size:12.5px; color:var(--muted); margin-top:6px; }
.hero-streak { display:none; font-size:12.5px; color:var(--accent); font-weight:700; margin-top:8px; }
```

- [ ] **Step 2: Markup del contador**

Ubicar:
```html
  <div class="hero">
    <div class="hero-title"><span class="flame" id="flame">🔥</span> Quemagrasa</div>
    <div class="hero-sub">Elige cómo quieres entrenar hoy</div>
  </div>
```
reemplazar por:
```html
  <div class="hero">
    <div class="hero-title"><span class="flame" id="flame">🔥</span> Quemagrasa</div>
    <div class="hero-sub">Elige cómo quieres entrenar hoy</div>
    <div class="hero-streak" id="hero-streak"></div>
  </div>
```

- [ ] **Step 3: Cargar `streak.js` y renderizar el contador**

Ubicar:
```html
<script src="weight-log.js"></script>
<script>
```
reemplazar por:
```html
<script src="weight-log.js"></script>
<script src="streak.js"></script>
<script>
```

Ubicar:
```javascript
try{ renderProgress(); }catch(e){}

/* easter egg: 7 taps al 🔥 en menos de 3s */
```
reemplazar por:
```javascript
try{ renderProgress(); }catch(e){}

function renderStreak(){
  const el = document.getElementById('hero-streak');
  if(!window.Streak){ el.style.display = 'none'; return; }
  const n = Streak.getCurrentStreak();
  if(n > 0){
    el.textContent = `🔥 ${n} día${n===1?'':'s'} seguido${n===1?'':'s'}`;
    el.style.display = '';
  } else {
    el.style.display = 'none';
  }
}
try{ renderStreak(); }catch(e){}

/* easter egg: 7 taps al 🔥 en menos de 3s */
```

- [ ] **Step 4: Verificar sintaxis**

```bash
node -e "
const fs=require('fs');
const src=fs.readFileSync('index.html','utf8');
const body=src.slice(src.lastIndexOf('<script>')+8, src.lastIndexOf('</script>'));
fs.writeFileSync('.syntax-check.js', body);
"
node --check .syntax-check.js
rm -f .syntax-check.js
```
Expected: sin salida (exit code 0).

- [ ] **Step 5: Prueba manual**

En `index.html`, con `qg_activity_log` vacío o inexistente, el contador de racha no debe aparecer. Simula una racha desde devtools:
```js
localStorage.setItem('qg_activity_log', JSON.stringify(['2026-08-20','2026-08-21','2026-08-22']));
```
(usando fechas reales de los últimos 3 días consecutivos terminando hoy) y recarga — debe verse `🔥 3 días seguidos` bajo el subtítulo. Prueba también singular con una sola fecha (solo hoy) — debe decir `🔥 1 día seguido`.

- [ ] **Step 6: Commit**

```bash
git add index.html
git commit -m "feat(racha): show current streak counter on the home screen"
```

---

### Task 7: Verificación end-to-end y push

**Files:** ninguno (solo verificación manual + push).

- [ ] **Step 1: Flujo completo real, un modo a la vez**

Sirve el sitio localmente y, empezando con `localStorage.clear()`:
1. Completa una actividad real en cada uno de los 5 modos (Casa, Gimnasio, Ejercicio rápido, un timer de cardio, Correr/Caminar) — puede ser en distintas pestañas del mismo origen.
2. Abre `index.html` y confirma que el contador dice `🔥 1 día seguido` (todas las actividades del mismo día real solo cuentan una vez).
3. Simula 2 días más consecutivos hacia atrás desde devtools (agregando fechas de ayer y antier a `qg_activity_log`) y recarga `index.html` — debe subir a `🔥 3 días seguidos`.
4. Simula un hueco (agrega una fecha de hace 5 días, sin nada entre medio) y recarga — la racha debe volver a mostrar solo lo que sigue siendo consecutivo hasta hoy (el día suelto de hace 5 días no debe sumarse).

- [ ] **Step 2: Confirmar que nada existente se rompió**

1. En `cardio.html`: confirma que su propio mensaje local "🔥 N días seguidos" (el preexistente, de `computeStreak()`) se sigue viendo igual en la pantalla de fin de sesión.
2. En `correr.html`, `rapido.html`, `entrenamientos.html`, `gimnasio.html`: confirma que sus flujos normales (kcal, calendario, ejercicios) no cambiaron en nada más allá del enganche de la racha.

- [ ] **Step 3: Push y confirmar en vivo**

```bash
git push
```

Espera a que se despliegue GitHub Pages y repite un flujo corto (completar 1 actividad, ver el contador subir en `index.html`) contra `https://pacoalvarezvivaldo-collab.github.io/quemagrasa/index.html` para confirmar que no hay sorpresas de CSP en producción (ninguno de los 5 archivos necesitó un dominio nuevo, pero se confirma igual siguiendo la práctica ya establecida en este proyecto).

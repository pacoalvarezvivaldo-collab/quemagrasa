# Ajustes de interfaz Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agregar una pantalla de Ajustes centralizada (`ajustes.html`) con 3 preferencias de interfaz (aviso de sonido, mantener pantalla encendida, pantalla completa) que aplican en toda la app, migrando los controles equivalentes que ya existen en `cardio.html` para que compartan la misma fuente de verdad; y extender el modo arcoíris (ya existente, oculto en `index.html`) para que su efecto de color se vea en las 8 páginas.

**Architecture:** Un módulo nuevo `prefs.js` (mismo patrón que `weight-log.js`/`streak.js`: IIFE, funciones puras + `localStorage`, expuesto como `window.Prefs`, sin red ni Supabase) centraliza lectura/escritura de las 3 preferencias más el efecto del modo arcoíris. Cada página de entrenamiento gatea sus beeps/Wake Lock existentes (o los agrega si no los tiene) leyendo de `Prefs`; las 8 páginas HTML aplican el modo arcoíris y re-piden pantalla completa en su primer toque.

**Tech Stack:** JS vanilla, `localStorage`, Fullscreen API, Wake Lock API — sin dependencias nuevas, mismo stack que el resto de la app.

## Global Constraints

- Las 3 preferencias son por dispositivo/navegador — viven solo en `localStorage`, nunca se sincronizan con Supabase.
- `prefs.js` no depende de ningún otro módulo (`sync.js`, `weight-log.js`, `streak.js`, `plan-engine.js`) — se puede cargar de primero o de último, da igual.
- No se toca el mecanismo de descubrimiento del modo arcoíris (las 6 zonas invisibles siguen solo en `index.html`) ni los ajustes propios de Cardio que no mapean a una preferencia global (números gigantes, ajuste ± en vivo, "propagar", calibración de máquina, `soundOn`).
- Cada archivo modificado se verifica con `node --check` (para los `.js`) o extrayendo su `<script>` inline a un archivo temporal en el scratchpad y corriendo `node --check` sobre él (para los `.html`) antes de dar el paso por bueno.
- Spec de referencia: `docs/superpowers/specs/2026-08-24-ajustes-design.md`.

---

### Task 1: Crear `prefs.js`

**Files:**
- Create: `prefs.js`

**Interfaces:**
- Produces: `window.Prefs = { getSoundWarningOn, setSoundWarningOn, getKeepScreenOn, setKeepScreenOn, getFullscreenPref, setFullscreenPref, maybeRequestFullscreen, isRainbowOn, hasRainbowBeenDiscovered, setRainbowOn, applyRainbowIfOn }` — usado por todas las tareas siguientes.

- [ ] **Step 1: Escribir `prefs.js`**

```js
/* =====================================================================
   prefs.js — preferencias de interfaz por dispositivo: aviso de sonido,
   mantener pantalla encendida, pantalla completa, modo arcoíris.
   Sin conocimiento de Supabase — viven 100% en localStorage, no se
   sincronizan entre dispositivos (son ajustes de interfaz, no datos de
   la cuenta). Compartido por las 8 páginas de la app.
   Ver docs/superpowers/specs/2026-08-24-ajustes-design.md
   ===================================================================== */
(function(){

const KEY_SOUND = 'qg_pref_sound', KEY_SCREEN = 'qg_pref_screen',
      KEY_FULLSCREEN = 'qg_pref_fullscreen', KEY_RAINBOW = 'qg_rainbow';

/* ---------- lógica pura (testeable sin DOM) ---------- */
function readBoolPref(raw, defaultValue){
  if(raw === null || raw === undefined) return defaultValue;
  return raw === '1';
}
/* ---------- fin lógica pura ---------- */

function getBool(key, defaultValue){
  try{ return readBoolPref(localStorage.getItem(key), defaultValue); }catch(e){ return defaultValue; }
}
function setBool(key, v){
  try{ localStorage.setItem(key, v ? '1' : '0'); }catch(e){}
}

function getSoundWarningOn(){ return getBool(KEY_SOUND, true); }
function setSoundWarningOn(v){ setBool(KEY_SOUND, v); }
function getKeepScreenOn(){ return getBool(KEY_SCREEN, true); }
function setKeepScreenOn(v){ setBool(KEY_SCREEN, v); }
function getFullscreenPref(){ return getBool(KEY_FULLSCREEN, false); }
function setFullscreenPref(v){ setBool(KEY_FULLSCREEN, v); }

function maybeRequestFullscreen(){
  if(!getFullscreenPref()) return;
  if(document.fullscreenElement) return;
  if(!document.documentElement.requestFullscreen) return;
  document.documentElement.requestFullscreen().catch(()=>{});
}

/* ---------- modo arcoíris: efecto compartido (el descubrimiento de las
   6 zonas vive solo en index.html, aquí solo el ciclo de color) ---------- */
let rainbowInterval = null, rainbowHue = 0;
function applyRainbowHue(){
  document.documentElement.style.setProperty('--accent', `hsl(${rainbowHue}, 85%, 55%)`);
  rainbowHue = (rainbowHue + 6) % 360;
}
function startRainbowEffect(){
  clearInterval(rainbowInterval);
  applyRainbowHue();
  rainbowInterval = setInterval(applyRainbowHue, 150);
}
function stopRainbowEffect(){
  clearInterval(rainbowInterval);
  rainbowInterval = null;
  document.documentElement.style.removeProperty('--accent');
}
function isRainbowOn(){ try{ return localStorage.getItem(KEY_RAINBOW)==='1'; }catch(e){ return false; } }
function hasRainbowBeenDiscovered(){ try{ return localStorage.getItem(KEY_RAINBOW) !== null; }catch(e){ return false; } }
function setRainbowOn(v){
  // nunca se borra la llave una vez descubierto (solo '1'/'0') — si no, "descubierto pero apagado"
  // sería indistinguible de "nunca descubierto" la próxima vez que se lea.
  try{ localStorage.setItem(KEY_RAINBOW, v ? '1' : '0'); }catch(e){}
  if(v) startRainbowEffect(); else stopRainbowEffect();
}
function applyRainbowIfOn(){ if(isRainbowOn()) startRainbowEffect(); }

window.Prefs = {
  getSoundWarningOn, setSoundWarningOn,
  getKeepScreenOn, setKeepScreenOn,
  getFullscreenPref, setFullscreenPref,
  maybeRequestFullscreen,
  isRainbowOn, hasRainbowBeenDiscovered, setRainbowOn, applyRainbowIfOn
};
})();
```

- [ ] **Step 2: Verificar sintaxis**

Run: `node --check "D:\Empresa\Programas\APP EJERCICIOS\quemagrasa\prefs.js"`
Expected: sin salida (sin errores).

- [ ] **Step 3: Assert-based check de la lógica pura**

Escribir en el scratchpad (`C:\Users\Paco\AppData\Local\Temp\claude\D--Empresa-Programas-APP-EJERCICIOS\<sesión>\scratchpad\prefs-smoke.js`):

```js
const assert = require('assert');
function readBoolPref(raw, defaultValue){
  if(raw === null || raw === undefined) return defaultValue;
  return raw === '1';
}
assert.strictEqual(readBoolPref(null, true), true);   // llave nunca guardada: usa el default
assert.strictEqual(readBoolPref(null, false), false);
assert.strictEqual(readBoolPref('1', false), true);   // guardado explícito en "1": true pase lo que pase el default
assert.strictEqual(readBoolPref('0', true), false);   // guardado explícito en "0": false pase lo que pase el default
console.log('prefs readBoolPref OK');

// modo arcoíris: "descubierto pero apagado" no debe verse igual que "nunca descubierto"
function hasRainbowBeenDiscovered(raw){ return raw !== null; }
function isRainbowOn(raw){ return raw === '1'; }
assert.strictEqual(hasRainbowBeenDiscovered(null), false);  // nunca tocado
assert.strictEqual(hasRainbowBeenDiscovered('0'), true);    // descubierto, apagado — SIGUE contando como descubierto
assert.strictEqual(isRainbowOn('0'), false);
assert.strictEqual(isRainbowOn('1'), true);
console.log('prefs rainbow discovered-vs-on OK');
```

Run: `node prefs-smoke.js` (desde el scratchpad)
Expected:
```
prefs readBoolPref OK
prefs rainbow discovered-vs-on OK
```

- [ ] **Step 4: Commit**

```bash
git add prefs.js
git commit -m "feat(prefs): add shared device-preferences module (sound, screen, fullscreen, rainbow)"
```

---

### Task 2: Casa y Gimnasio (`plan-engine.js`) — gatear beep/vibrate y agregar Wake Lock

**Files:**
- Modify: `plan-engine.js:88-99` (funciones `beep`/`vibrate` existentes), `plan-engine.js:202-213` (`openDay`), `plan-engine.js:174-178` (`backToCalendar`), `plan-engine.js:346-353` (`completeSession`), `plan-engine.js:324-337` (`tickRest`)
- Modify: `entrenamientos.html:184-186`, `gimnasio.html:181-183` (agregar `<script src="prefs.js"></script>`)

**Interfaces:**
- Consumes: `window.Prefs.getSoundWarningOn()`, `window.Prefs.getKeepScreenOn()`, `window.Prefs.maybeRequestFullscreen()`, `window.Prefs.applyRainbowIfOn()` (de Task 1)

- [ ] **Step 1: Agregar `<script src="prefs.js">` antes de `plan-engine.js` en ambas páginas**

En `entrenamientos.html`, reemplazar:
```html
<script src="sync.js"></script>
<script src="streak.js"></script>
<script src="plan-engine.js"></script>
```
por:
```html
<script src="sync.js"></script>
<script src="streak.js"></script>
<script src="prefs.js"></script>
<script src="plan-engine.js"></script>
```

En `gimnasio.html`, el mismo cambio (mismas 3 líneas exactas).

- [ ] **Step 2: Gatear el beep/vibrate de aviso en `tickRest()`**

En `plan-engine.js`, reemplazar:
```js
function tickRest(){
  if(paused) return;
  restLeft = Math.max(0, Math.ceil((restEndAt-Date.now())/1000));
  if(!restWarned && restLeft<=10 && restLeft>0){
    restWarned = true; vibrate([90,70,90]); beep(1320,0.1,0.28);
  }
```
por:
```js
function tickRest(){
  if(paused) return;
  restLeft = Math.max(0, Math.ceil((restEndAt-Date.now())/1000));
  if(!restWarned && restLeft<=10 && restLeft>0){
    restWarned = true;
    if(!window.Prefs || Prefs.getSoundWarningOn()){ vibrate([90,70,90]); beep(1320,0.1,0.28); }
  }
```

- [ ] **Step 3: Agregar Wake Lock — variable y funciones**

En `plan-engine.js`, ubicar (justo después de `function vibrate(p){...}`, línea 99):
```js
function vibrate(p){ if(navigator.vibrate) try{ navigator.vibrate(p); }catch(e){} }
```
agregar debajo:
```js
function vibrate(p){ if(navigator.vibrate) try{ navigator.vibrate(p); }catch(e){} }

/* ---------- pantalla encendida durante el ejercicio ---------- */
let wakeLock = null;
async function requestWakeLock(){
  if(wakeLock) return;
  if(window.Prefs && !Prefs.getKeepScreenOn()) return;
  if('wakeLock' in navigator) try{ wakeLock = await navigator.wakeLock.request('screen'); }catch(e){}
}
async function releaseWakeLock(){
  if(wakeLock){ try{ await wakeLock.release(); }catch(e){} wakeLock = null; }
}
document.addEventListener('visibilitychange', ()=>{
  if(document.visibilityState==='visible' && currentSession.length) requestWakeLock();
});
```

- [ ] **Step 4: Pedir/soltar el Wake Lock al abrir/cerrar un día**

En `plan-engine.js`, en `openDay(day)`, reemplazar:
```js
function openDay(day){
  stopRestTimer();
  currentDay = day;
  currentSession = exercisesForDay(day);
  currentIdx = 0;
  CURRENT_DAY_SAVED = day;
  saveCurrentDay(day);
  document.getElementById('upcoming-wrap').style.display = '';
  document.getElementById('player-controls').style.display = 'flex';
  showScreen('player');
  maybeShowEgg(day, renderExercise);
}
```
por:
```js
function openDay(day){
  stopRestTimer();
  currentDay = day;
  currentSession = exercisesForDay(day);
  currentIdx = 0;
  CURRENT_DAY_SAVED = day;
  saveCurrentDay(day);
  document.getElementById('upcoming-wrap').style.display = '';
  document.getElementById('player-controls').style.display = 'flex';
  showScreen('player');
  requestWakeLock();
  maybeShowEgg(day, renderExercise);
}
```

En `backToCalendar()`, reemplazar:
```js
function backToCalendar(){
  stopRestTimer();
  renderCalendar();
  showScreen('calendar');
}
```
por:
```js
function backToCalendar(){
  stopRestTimer();
  releaseWakeLock();
  renderCalendar();
  showScreen('calendar');
}
```

En `completeSession()`, reemplazar:
```js
function completeSession(){
  COMPLETED.add(currentDay);
  saveCompleted();
  if(window.Streak) Streak.recordActivity();
  if(CONFIG.onActivityRecorded) CONFIG.onActivityRecorded();
  document.getElementById('complete-day').textContent = currentDay;
  showScreen('complete');
}
```
por:
```js
function completeSession(){
  COMPLETED.add(currentDay);
  saveCompleted();
  releaseWakeLock();
  if(window.Streak) Streak.recordActivity();
  if(CONFIG.onActivityRecorded) CONFIG.onActivityRecorded();
  document.getElementById('complete-day').textContent = currentDay;
  showScreen('complete');
}
```

- [ ] **Step 5: Modo arcoíris + pantalla completa en el primer toque, en ambas páginas**

En `entrenamientos.html`, reemplazar:
```js
    pullRemoteState: async () => (window.QuemaSync && QuemaSync.isSignedIn()) ? await QuemaSync.pullUserState() : null
  });
})();
```
por:
```js
    pullRemoteState: async () => (window.QuemaSync && QuemaSync.isSignedIn()) ? await QuemaSync.pullUserState() : null
  });
})();

try{ Prefs.applyRainbowIfOn(); }catch(e){}
document.addEventListener('click', ()=>{ try{ Prefs.maybeRequestFullscreen(); }catch(e){} }, {once:true});
```

En `gimnasio.html`, reemplazar:
```js
});
}catch(e){ console.error('Gimnasio init falló:', e); }
})();
```
por:
```js
});
}catch(e){ console.error('Gimnasio init falló:', e); }
})();

try{ Prefs.applyRainbowIfOn(); }catch(e){}
document.addEventListener('click', ()=>{ try{ Prefs.maybeRequestFullscreen(); }catch(e){} }, {once:true});
```

- [ ] **Step 6: Verificar sintaxis**

Run: `node --check "D:\Empresa\Programas\APP EJERCICIOS\quemagrasa\plan-engine.js"`
Expected: sin salida.

Extraer y verificar el `<script>` inline de `entrenamientos.html` y `gimnasio.html` (mismo método de las tareas de hoy: regex `<script(?![^>]*\bsrc=)[^>]*>([\s\S]*?)<\/script>` a un archivo temporal del scratchpad + `node --check`).
Expected: sin salida en ambos.

- [ ] **Step 7: Commit**

```bash
git add plan-engine.js entrenamientos.html gimnasio.html
git commit -m "feat(prefs): wire sound warning + wake lock + fullscreen + rainbow into Casa/Gimnasio"
```

---

### Task 3: Ejercicio rápido (`rapido.html`) — gatear el beep de aviso y el Wake Lock existentes

**Files:**
- Modify: `rapido.html:114-115` (agregar `<script src="prefs.js">`), `rapido.html:237-239` (`requestWakeLock`), `rapido.html:259-260` (aviso en `tick()`)

**Interfaces:**
- Consumes: `window.Prefs.getSoundWarningOn()`, `window.Prefs.getKeepScreenOn()`, `window.Prefs.maybeRequestFullscreen()`, `window.Prefs.applyRainbowIfOn()` (de Task 1)

- [ ] **Step 1: Agregar `<script src="prefs.js">`**

Reemplazar:
```html
<script src="sync.js"></script>
<script src="streak.js"></script>
```
por:
```html
<script src="sync.js"></script>
<script src="streak.js"></script>
<script src="prefs.js"></script>
```

- [ ] **Step 2: Gatear el Wake Lock existente**

Reemplazar:
```js
async function requestWakeLock(){
  if('wakeLock' in navigator) try{ wakeLock = await navigator.wakeLock.request('screen'); }catch(e){}
}
```
por:
```js
async function requestWakeLock(){
  if(wakeLock) return;
  if(window.Prefs && !Prefs.getKeepScreenOn()) return;
  if('wakeLock' in navigator) try{ wakeLock = await navigator.wakeLock.request('screen'); }catch(e){}
}
```

- [ ] **Step 3: Gatear el beep de aviso existente**

Reemplazar (en `tick()`):
```js
  if(phase==='rest' && !phaseWarned && left<=10 && left>0){
    phaseWarned = true; vibrate([90,70,90]); beep(1320,0.1,0.28);
  }
```
por:
```js
  if(phase==='rest' && !phaseWarned && left<=10 && left>0){
    phaseWarned = true;
    if(!window.Prefs || Prefs.getSoundWarningOn()){ vibrate([90,70,90]); beep(1320,0.1,0.28); }
  }
```

- [ ] **Step 4: Modo arcoíris + pantalla completa en el primer toque**

Reemplazar:
```js
/* ---------- init ---------- */
if(window.QuemaSync) QuemaSync.initSilent(); // sin botón propio: hereda la sesión ya persistida por Supabase, si existe
ensurePool();
```
por:
```js
/* ---------- init ---------- */
if(window.QuemaSync) QuemaSync.initSilent(); // sin botón propio: hereda la sesión ya persistida por Supabase, si existe
ensurePool();
try{ Prefs.applyRainbowIfOn(); }catch(e){}
document.addEventListener('click', ()=>{ try{ Prefs.maybeRequestFullscreen(); }catch(e){} }, {once:true});
```

- [ ] **Step 5: Verificar sintaxis**

Extraer el `<script>` inline de `rapido.html` a un archivo temporal del scratchpad y correr `node --check`.
Expected: sin salida.

- [ ] **Step 6: Commit**

```bash
git add rapido.html
git commit -m "feat(prefs): wire sound warning + wake lock + fullscreen + rainbow into rapido.html"
```

---

### Task 4: Correr/Caminar (`correr.html`) — gatear el Wake Lock existente

**Files:**
- Modify: `correr.html:202-204` (agregar `<script src="prefs.js">`), `correr.html:277-280` (`requestWakeLock`)

**Interfaces:**
- Consumes: `window.Prefs.getKeepScreenOn()`, `window.Prefs.maybeRequestFullscreen()`, `window.Prefs.applyRainbowIfOn()` (de Task 1)

- [ ] **Step 1: Agregar `<script src="prefs.js">`**

Reemplazar:
```html
<script src="sync.js"></script>
<script src="weight-log.js"></script>
<script src="streak.js"></script>
```
por:
```html
<script src="sync.js"></script>
<script src="weight-log.js"></script>
<script src="streak.js"></script>
<script src="prefs.js"></script>
```

- [ ] **Step 2: Gatear el Wake Lock existente**

Reemplazar:
```js
async function requestWakeLock(){
  if(wakeLock) return;
  if('wakeLock' in navigator) try{ wakeLock = await navigator.wakeLock.request('screen'); }catch(e){}
}
```
por:
```js
async function requestWakeLock(){
  if(wakeLock) return;
  if(window.Prefs && !Prefs.getKeepScreenOn()) return;
  if('wakeLock' in navigator) try{ wakeLock = await navigator.wakeLock.request('screen'); }catch(e){}
}
```

- [ ] **Step 3: Modo arcoíris + pantalla completa en el primer toque**

Reemplazar (al final del archivo):
```js
  const remote = await QuemaSync.pullUserState();
  if(remote && remote.weightKg != null){
    userWeight = remote.weightKg;
    document.getElementById('weight-input').value = remote.weightKg;
  }
})();
</script>
```
por:
```js
  const remote = await QuemaSync.pullUserState();
  if(remote && remote.weightKg != null){
    userWeight = remote.weightKg;
    document.getElementById('weight-input').value = remote.weightKg;
  }
})();

try{ Prefs.applyRainbowIfOn(); }catch(e){}
document.addEventListener('click', ()=>{ try{ Prefs.maybeRequestFullscreen(); }catch(e){} }, {once:true});
</script>
```

- [ ] **Step 4: Verificar sintaxis**

Extraer el `<script>` inline de `correr.html` a un archivo temporal del scratchpad y correr `node --check`.
Expected: sin salida.

- [ ] **Step 5: Commit**

```bash
git add correr.html
git commit -m "feat(prefs): wire keep-screen-on + fullscreen + rainbow into correr.html"
```

---

### Task 5: Cardio (`cardio.html`) — migrar `bgMode` y `preWarn` a las llaves compartidas

**Files:**
- Modify: `cardio.html:447-448` (agregar `<script src="prefs.js">`), `cardio.html:924-932` (`toggleBgMode`/`togglePreWarn`), `cardio.html:1017-1036` (`loadSettings`)

**Interfaces:**
- Consumes: `window.Prefs.getKeepScreenOn/setKeepScreenOn/getSoundWarningOn/setSoundWarningOn/maybeRequestFullscreen/applyRainbowIfOn` (de Task 1)

**Nota:** `soundOn` (el mute general de todos los beeps de Cardio, incluido el conteo al final de cada intervalo) **no se toca** — se queda como ajuste propio de Cardio, sin relación con `prefs.js`.

- [ ] **Step 1: Agregar `<script src="prefs.js">`**

Reemplazar:
```html
<script src="sync.js"></script>
<script src="streak.js"></script>
```
por:
```html
<script src="sync.js"></script>
<script src="streak.js"></script>
<script src="prefs.js"></script>
```

- [ ] **Step 2: `toggleBgMode`/`togglePreWarn` escriben a la llave compartida**

Reemplazar:
```js
function toggleBgMode(){ bgMode=!bgMode;
  document.getElementById('bgmode-row').classList.toggle('on',bgMode);
  if(running){ if(bgMode) startKeepAlive(); else { silentAudio.pause(); releaseWakeLock(); } } }
function toggleSound(){ soundOn=!soundOn;
  document.getElementById('sound-row').classList.toggle('on',soundOn);
  if(soundOn){ initAudio(); beep(660,0.1,0.2); } }
function togglePreWarn(){ preWarn=!preWarn;
  document.getElementById('prewarn-row').classList.toggle('on',preWarn);
  if(!preWarn) hidePreWarn(); }
```
por:
```js
function toggleBgMode(){ bgMode=!bgMode;
  if(window.Prefs) Prefs.setKeepScreenOn(bgMode);
  document.getElementById('bgmode-row').classList.toggle('on',bgMode);
  if(running){ if(bgMode) startKeepAlive(); else { silentAudio.pause(); releaseWakeLock(); } } }
function toggleSound(){ soundOn=!soundOn;
  document.getElementById('sound-row').classList.toggle('on',soundOn);
  if(soundOn){ initAudio(); beep(660,0.1,0.2); } }
function togglePreWarn(){ preWarn=!preWarn;
  if(window.Prefs) Prefs.setSoundWarningOn(preWarn);
  document.getElementById('prewarn-row').classList.toggle('on',preWarn);
  if(!preWarn) hidePreWarn(); }
```

- [ ] **Step 3: `loadSettings()` lee `bgMode`/`preWarn` de la llave compartida en vez del blob `qg_config`**

Reemplazar:
```js
  ['bgMode','soundOn','preWarn','adjustOn','propagate','gymEnabled'].forEach(k=>{
    if(typeof c[k]==='boolean'){
      if(k==='bgMode') bgMode=c[k]; if(k==='soundOn') soundOn=c[k];
      if(k==='preWarn') preWarn=c[k]; if(k==='adjustOn') adjustOn=c[k];
      if(k==='propagate') propagate=c[k]; if(k==='gymEnabled') gymEnabled=c[k];
    }
  });
  if(!('wakeLock' in navigator)) bgMode=false;
```
por:
```js
  ['soundOn','adjustOn','propagate','gymEnabled'].forEach(k=>{
    if(typeof c[k]==='boolean'){
      if(k==='soundOn') soundOn=c[k]; if(k==='adjustOn') adjustOn=c[k];
      if(k==='propagate') propagate=c[k]; if(k==='gymEnabled') gymEnabled=c[k];
    }
  });
  if(window.Prefs){ bgMode = Prefs.getKeepScreenOn(); preWarn = Prefs.getSoundWarningOn(); }
  if(!('wakeLock' in navigator)) bgMode=false;
```

**Nota para quien implemente:** `bgMode` y `preWarn` salen del `forEach` de arriba porque ya no viven en el blob `qg_config` — vienen de `Prefs` directo. `currentConfig()` (línea ~1000-1005) sigue guardando `bgMode`/`preWarn` dentro del blob por compatibilidad hacia atrás (no rompe nada dejarlos ahí, simplemente ya no se leen de vuelta desde ese blob) — no hace falta tocar esa función.

- [ ] **Step 4: Modo arcoíris + pantalla completa en el primer toque**

Reemplazar:
```js
  sessions=(await Store.get(KEY_HIST))||[];
  renderHistory();
})();
</script>
```
por:
```js
  sessions=(await Store.get(KEY_HIST))||[];
  renderHistory();
})();

try{ Prefs.applyRainbowIfOn(); }catch(e){}
document.addEventListener('click', ()=>{ try{ Prefs.maybeRequestFullscreen(); }catch(e){} }, {once:true});
</script>
```

- [ ] **Step 5: Verificar sintaxis**

Extraer el `<script>` inline de `cardio.html` a un archivo temporal del scratchpad y correr `node --check`.
Expected: sin salida.

- [ ] **Step 6: Commit**

```bash
git add cardio.html
git commit -m "feat(prefs): migrate cardio.html bgMode/preWarn to shared prefs keys, add fullscreen+rainbow hooks"
```

---

### Task 6: `index.html` — mover el efecto del modo arcoíris a `prefs.js`, agregar pantalla completa y el enlace a Ajustes

**Files:**
- Modify: `index.html:231-233` (agregar `<script src="prefs.js">`), `index.html:196-200` (agregar renglón "Ajustes" al final de la lista), `index.html:533-574` (refactor del modo arcoíris)

**Interfaces:**
- Consumes: `window.Prefs.applyRainbowIfOn/setRainbowOn/isRainbowOn/maybeRequestFullscreen` (de Task 1)

- [ ] **Step 1: Agregar `<script src="prefs.js">`**

Reemplazar:
```html
<script src="sync.js"></script>
<script src="weight-log.js"></script>
<script src="streak.js"></script>
```
por:
```html
<script src="sync.js"></script>
<script src="weight-log.js"></script>
<script src="streak.js"></script>
<script src="prefs.js"></script>
```

- [ ] **Step 2: Agregar el renglón "Ajustes" al final de la lista de modos**

Reemplazar:
```html
    <a class="mode-row" href="estadisticas.html">
      <span class="mode-icon">📈</span>
      <span class="mode-text"><span class="mode-name">Estadísticas</span><span class="mode-sub">Totales de los 5 modos</span></span>
      <span class="mode-chevron">›</span>
    </a>
  </div>
</div>
```
por:
```html
    <a class="mode-row" href="estadisticas.html">
      <span class="mode-icon">📈</span>
      <span class="mode-text"><span class="mode-name">Estadísticas</span><span class="mode-sub">Totales de los 5 modos</span></span>
      <span class="mode-chevron">›</span>
    </a>
    <a class="mode-row" href="ajustes.html">
      <span class="mode-icon">⚙️</span>
      <span class="mode-text"><span class="mode-name">Ajustes</span><span class="mode-sub">Sonido, pantalla, color</span></span>
      <span class="mode-chevron">›</span>
    </a>
  </div>
</div>
```

- [ ] **Step 3: Mover el efecto de color a `Prefs`, dejar el descubrimiento en `index.html`**

Reemplazar:
```js
/* easter egg oculto: tocar las 6 zonas invisibles en orden (izquierda a derecha) alterna "modo arcoíris" */
const KEY_RAINBOW = 'qg_rainbow';
let rainbowExpected = 0, rainbowResetTimer = null, rainbowInterval = null, rainbowHue = 0;

function applyRainbowHue(){
  document.documentElement.style.setProperty('--accent', `hsl(${rainbowHue}, 85%, 55%)`);
  rainbowHue = (rainbowHue + 6) % 360;
}
function startRainbow(){
  clearInterval(rainbowInterval);
  applyRainbowHue();
  rainbowInterval = setInterval(applyRainbowHue, 150);
}
function stopRainbow(){
  clearInterval(rainbowInterval);
  rainbowInterval = null;
  document.documentElement.style.removeProperty('--accent');
}
function toggleRainbow(){
  if(rainbowInterval){ stopRainbow(); try{ localStorage.removeItem(KEY_RAINBOW); }catch(e){} }
  else{ startRainbow(); try{ localStorage.setItem(KEY_RAINBOW,'1'); }catch(e){} }
}

document.querySelectorAll('.rainbow-zone').forEach(zone=>{
  zone.addEventListener('click', ()=>{
    const z = parseInt(zone.dataset.z, 10);
    clearTimeout(rainbowResetTimer);
    if(z === rainbowExpected){
      rainbowExpected++;
      if(rainbowExpected >= 6){
        rainbowExpected = 0;
        toggleRainbow();
        return;
      }
      rainbowResetTimer = setTimeout(()=>{ rainbowExpected = 0; }, 4000);
    } else {
      rainbowExpected = (z===0) ? 1 : 0;
      if(rainbowExpected) rainbowResetTimer = setTimeout(()=>{ rainbowExpected = 0; }, 4000);
    }
  });
});

try{ if(localStorage.getItem(KEY_RAINBOW)==='1') startRainbow(); }catch(e){}
```
por:
```js
/* easter egg oculto: tocar las 6 zonas invisibles en orden (izquierda a derecha) alterna "modo arcoíris".
   El efecto de color (el ciclo en sí) vive en prefs.js, compartido con las demás páginas —
   aquí solo queda el mecanismo secreto de descubrimiento. */
let rainbowExpected = 0, rainbowResetTimer = null;

document.querySelectorAll('.rainbow-zone').forEach(zone=>{
  zone.addEventListener('click', ()=>{
    const z = parseInt(zone.dataset.z, 10);
    clearTimeout(rainbowResetTimer);
    if(z === rainbowExpected){
      rainbowExpected++;
      if(rainbowExpected >= 6){
        rainbowExpected = 0;
        Prefs.setRainbowOn(!Prefs.isRainbowOn());
        return;
      }
      rainbowResetTimer = setTimeout(()=>{ rainbowExpected = 0; }, 4000);
    } else {
      rainbowExpected = (z===0) ? 1 : 0;
      if(rainbowExpected) rainbowResetTimer = setTimeout(()=>{ rainbowExpected = 0; }, 4000);
    }
  });
});

try{ Prefs.applyRainbowIfOn(); }catch(e){}
```

- [ ] **Step 4: Pantalla completa en el primer toque**

Ubicar el listener de ripple ambient existente:
```js
document.addEventListener('click', e=>{
  if(e.target.closest('a, button, input, .progress-card, .progress-goal-link, .rainbow-zone')) return;
  const r = document.createElement('div');
```
Justo arriba de ese bloque, agregar (una sola vez, no dentro del listener):
```js
document.addEventListener('click', ()=>{ try{ Prefs.maybeRequestFullscreen(); }catch(e){} }, {once:true});
```

- [ ] **Step 5: Verificar sintaxis**

Extraer el `<script>` inline de `index.html` a un archivo temporal del scratchpad y correr `node --check`.
Expected: sin salida.

- [ ] **Step 6: Commit**

```bash
git add index.html
git commit -m "feat(prefs): move rainbow effect to prefs.js, add fullscreen hook and Ajustes entry"
```

---

### Task 7: `estadisticas.html` — modo arcoíris y pantalla completa

**Files:**
- Modify: `estadisticas.html:94` (agregar `<script src="prefs.js">`), `estadisticas.html:170-174` (final del script)

**Interfaces:**
- Consumes: `window.Prefs.applyRainbowIfOn/maybeRequestFullscreen` (de Task 1)

- [ ] **Step 1: Agregar `<script src="prefs.js">`**

Reemplazar:
```html
<script src="streak.js"></script>
<script>
```
por:
```html
<script src="streak.js"></script>
<script src="prefs.js"></script>
<script>
```

- [ ] **Step 2: Aplicar modo arcoíris y pantalla completa**

Reemplazar:
```js
renderSummary();
renderCorrer();
renderCardio();
renderPlan('ent_', 'casa-level', 'casa-days');
renderPlan('gym_', 'gimnasio-level', 'gimnasio-days');
```
por:
```js
renderSummary();
renderCorrer();
renderCardio();
renderPlan('ent_', 'casa-level', 'casa-days');
renderPlan('gym_', 'gimnasio-level', 'gimnasio-days');
try{ Prefs.applyRainbowIfOn(); }catch(e){}
document.addEventListener('click', ()=>{ try{ Prefs.maybeRequestFullscreen(); }catch(e){} }, {once:true});
```

- [ ] **Step 3: Verificar sintaxis**

Extraer el `<script>` inline de `estadisticas.html` a un archivo temporal del scratchpad y correr `node --check`.
Expected: sin salida.

- [ ] **Step 4: Commit**

```bash
git add estadisticas.html
git commit -m "feat(prefs): apply rainbow mode and fullscreen hook on estadisticas.html"
```

---

### Task 8: Crear `ajustes.html`

**Files:**
- Create: `ajustes.html`

**Interfaces:**
- Consumes: `window.Prefs.*` (de Task 1)

- [ ] **Step 1: Escribir `ajustes.html`**

```html
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="Ajustes">
<meta name="mobile-web-app-capable" content="yes">
<meta name="theme-color" content="#0d0d0f">
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src https://fonts.gstatic.com; script-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'none'; frame-ancestors 'none'; base-uri 'none'; form-action 'none';">
<title>Ajustes · Quemagrasa</title>
<link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Syne:wght@400;700;800&display=swap" rel="stylesheet">
<style>
*, *::before, *::after { box-sizing:border-box; margin:0; padding:0; }
:root {
  --bg:#0d0d0f; --surface:#18181c; --surface2:#222228;
  --border:rgba(255,255,255,0.08); --text:#f0efe8; --muted:#777;
  --accent:#EF9F27; --accent-text:#000;
  --safe-top: env(safe-area-inset-top, 0px);
  --safe-bot: env(safe-area-inset-bottom, 0px);
}
html, body { height:100%; background:var(--bg); overscroll-behavior:none; }
body { color:var(--text); font-family:'Syne',sans-serif; padding-top:var(--safe-top);
  -webkit-user-select:none; user-select:none; touch-action:manipulation; }

.topbar { display:flex; align-items:center; justify-content:space-between;
  padding:12px 14px 9px 18px; border-bottom:1px solid var(--border); }
.topbar-title { font-size:13px; font-weight:800; letter-spacing:.03em; }
.gear-btn { background:var(--surface2); border:1px solid var(--border); color:var(--text);
  width:34px; height:34px; border-radius:10px; font-size:15px; cursor:pointer;
  display:flex; align-items:center; justify-content:center; text-decoration:none;
  -webkit-tap-highlight-color:transparent; }

.wrap { max-width:480px; margin:0 auto; padding:20px calc(20px + env(safe-area-inset-right,0px)) calc(24px + var(--safe-bot)); }

.set-group { margin-bottom:18px; }
.set-label { font-size:10px; font-weight:700; letter-spacing:.1em; text-transform:uppercase; color:var(--muted); margin-bottom:8px; }

.toggle-row.disabled { opacity:.45; pointer-events:none; }
.toggle-row { display:flex; align-items:center; justify-content:space-between; background:var(--surface2);
  border:1px solid var(--border); border-radius:12px; padding:11px 14px; cursor:pointer;
  -webkit-tap-highlight-color:transparent; margin-bottom:7px; gap:10px; }
.toggle-txt { font-size:12.5px; font-weight:700; }
.toggle-sub { font-size:10px; color:var(--muted); font-weight:400; display:block; margin-top:2px; }
.toggle-sw { width:44px; height:25px; border-radius:20px; background:var(--border); position:relative; transition:background .2s; flex-shrink:0; }
.toggle-sw::after { content:""; position:absolute; top:3px; left:3px; width:19px; height:19px; border-radius:50%;
  background:var(--text); transition:transform .2s; }
.toggle-row.on .toggle-sw { background:var(--accent); }
.toggle-row.on .toggle-sw::after { transform:translateX(19px); }

.rainbow-note { font-size:12.5px; color:var(--muted); background:var(--surface2); border-radius:12px; padding:12px 14px; }
</style>
</head>
<body>

<div class="topbar">
  <a class="gear-btn" href="index.html" title="Volver al inicio">←</a>
  <span class="topbar-title">⚙️ Ajustes</span>
  <span style="width:34px"></span>
</div>

<div class="wrap">
  <div class="set-group">
    <div class="set-label">Sonido y pantalla</div>
    <div class="toggle-row" id="sound-row" onclick="toggleSound()">
      <span class="toggle-txt">Aviso de siguiente ejercicio
        <span class="toggle-sub">Beep + vibración 10s antes de que acabe el descanso</span></span>
      <span class="toggle-sw"></span>
    </div>
    <div class="toggle-row" id="screen-row" onclick="toggleScreen()">
      <span class="toggle-txt">Mantener pantalla encendida
        <span class="toggle-sub">Evita que el celular se bloquee mientras entrenas</span></span>
      <span class="toggle-sw"></span>
    </div>
    <div class="toggle-row" id="fullscreen-row" onclick="toggleFullscreen()">
      <span class="toggle-txt">Pantalla completa
        <span class="toggle-sub">Oculta la barra del navegador — se re-activa sola al tocar</span></span>
      <span class="toggle-sw"></span>
    </div>
  </div>

  <div class="set-group">
    <div class="set-label">Color</div>
    <div id="rainbow-box"></div>
  </div>
</div>

<script src="prefs.js"></script>
<script>
/* =====================================================================
   ajustes.html — pantalla centralizada de preferencias de interfaz.
   Lee y escribe directo en prefs.js, sin lógica propia de negocio.
   Ver docs/superpowers/specs/2026-08-24-ajustes-design.md
   ===================================================================== */

function syncToggleUI(){
  document.getElementById('sound-row').classList.toggle('on', Prefs.getSoundWarningOn());
  document.getElementById('screen-row').classList.toggle('on', Prefs.getKeepScreenOn());
  document.getElementById('fullscreen-row').classList.toggle('on', Prefs.getFullscreenPref());
  if(!('wakeLock' in navigator)){
    document.getElementById('screen-row').classList.add('disabled');
  }
  if(!document.documentElement.requestFullscreen){
    document.getElementById('fullscreen-row').classList.add('disabled');
  }
}

function toggleSound(){ Prefs.setSoundWarningOn(!Prefs.getSoundWarningOn()); syncToggleUI(); }
function toggleScreen(){ Prefs.setKeepScreenOn(!Prefs.getKeepScreenOn()); syncToggleUI(); }
function toggleFullscreen(){
  const next = !Prefs.getFullscreenPref();
  Prefs.setFullscreenPref(next);
  syncToggleUI();
  if(next) Prefs.maybeRequestFullscreen(); // el toque en este mismo botón cuenta como gesto del usuario
}

function renderRainbowBox(){
  const box = document.getElementById('rainbow-box');
  if(!Prefs.hasRainbowBeenDiscovered()){
    box.innerHTML = '<div class="rainbow-note">🔒 Hay un modo de color secreto por descubrir en Inicio</div>';
    return;
  }
  box.innerHTML = `<div class="toggle-row on" id="rainbow-row" onclick="toggleRainbowMode()">
    <span class="toggle-txt">🌈 Modo arcoíris</span>
    <span class="toggle-sw"></span>
  </div>`;
  document.getElementById('rainbow-row').classList.toggle('on', Prefs.isRainbowOn());
}
function toggleRainbowMode(){
  Prefs.setRainbowOn(!Prefs.isRainbowOn());
  renderRainbowBox();
}

syncToggleUI();
renderRainbowBox();
try{ Prefs.applyRainbowIfOn(); }catch(e){}
document.addEventListener('click', ()=>{ try{ Prefs.maybeRequestFullscreen(); }catch(e){} }, {once:true});
</script>
</body>
</html>
```

- [ ] **Step 2: Verificar sintaxis**

Extraer el `<script>` inline de `ajustes.html` a un archivo temporal del scratchpad y correr `node --check`.
Expected: sin salida.

- [ ] **Step 3: Commit**

```bash
git add ajustes.html
git commit -m "feat(ajustes): add centralized interface settings page"
```

---

### Task 9: Verificación end-to-end y push

**Files:** ninguno (solo pruebas manuales + push)

- [ ] **Step 1: Verificación estática de las 9 páginas tocadas**

Correr `node --check` (directo o vía extracción de `<script>` inline, según el archivo) sobre: `prefs.js`, `plan-engine.js`, `entrenamientos.html`, `gimnasio.html`, `rapido.html`, `correr.html`, `cardio.html`, `index.html`, `estadisticas.html`, `ajustes.html`.
Expected: sin salida en las 10.

- [ ] **Step 2: Push**

```bash
git push
```

- [ ] **Step 3: Verificación manual en el link publicado (guía para el usuario, con Ctrl+Shift+R en cada pantalla nueva por la caché)**

1. Entrar a Ajustes desde Inicio, confirmar que los 3 toggles arrancan en su default (Aviso: on, Pantalla encendida: on, Pantalla completa: off) y que muestra "🔒 Hay un modo de color secreto..." si el arcoíris nunca se ha descubierto.
2. Apagar "Aviso de siguiente ejercicio" en Ajustes → completar un ejercicio en Gimnasio y en Rápido hasta el descanso → confirmar que NO suena ni vibra a los 10s. Prender de nuevo → confirmar que sí suena en ambos.
3. Apagar "Mantener pantalla encendida" en Ajustes → abrir Correr y empezar a rastrear → confirmar (con las dev tools o esperando a que la pantalla se apague sola) que ya no se mantiene forzada. Prender de nuevo.
4. Prender "Pantalla completa" en Ajustes (el propio toque activa fullscreen ahí mismo) → navegar a Casa → tocar cualquier botón → confirmar que vuelve a entrar en pantalla completa sola.
5. En Cardio, abrir su propio Ajustes interno → confirmar que "Mantener pantalla encendida" y "Aviso 10s antes del sprint" reflejan el mismo estado que se dejó en `ajustes.html` (y viceversa: cambiarlo ahí y confirmar que se refleja en `ajustes.html`).
6. Si el modo arcoíris ya estaba descubierto de antes: confirmar que el color cicla también en Correr/Cardio/Gimnasio/Rápido/Estadísticas, no solo en Inicio. Si no estaba descubierto: hacer la secuencia de 6 toques en Inicio y confirmar que ahora sí aparece el toggle en Ajustes.

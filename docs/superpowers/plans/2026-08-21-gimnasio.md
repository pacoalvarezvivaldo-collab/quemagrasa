# Modo Gimnasio Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extraer el motor del plan de 30 días de `entrenamientos.html` a un archivo compartido `plan-engine.js`, y usarlo para construir un nuevo modo "Gimnasio" (`gimnasio.html`) con ejercicios que usan equipo, sin cambiar el comportamiento de Casa.

**Architecture:** `plan-engine.js` es un script clásico (no módulo, sin build step) que expone `window.PlanEngine.init(config)`. Sus funciones de UI (`pickLevel`, `openDayFlow`, `markDone`, etc.) quedan globales — igual que hoy — porque el HTML las invoca vía `onclick="..."`. Todo lo específico de Casa (filtro de equipo "sin equipo", tip de silla, hooks de sync con Supabase) se queda **fuera** del motor, pasado como funciones opcionales en `config`; el motor nunca referencia `QuemaSync` ni conoce reglas de filtrado.

**Tech Stack:** HTML/CSS/JS puro (sin build), mismo CDN de ejercicios (`JahelCuadrado/ExerciseGymGifsDB`), Node.js solo para verificación (`node --check`, scripts de assert).

## Global Constraints

- `plan-engine.js` no debe referenciar `QuemaSync`, `sync.js`, ni ninguna regla de filtrado de equipo directamente — todo eso entra por `config` como funciones opcionales (`excludeExercise`, `chairTip`, `onSaveLevel`, `onSaveCompleted`, `onSaveCurrentDay`, `pullRemoteState`).
- El refactor de `entrenamientos.html` no debe cambiar ningún comportamiento observable: mismas llaves de `localStorage` (`ent_level`, `ent_completed`, `ent_current`), mismo flujo, mismo easter egg, mismo comportamiento de sync ya cableado.
- `gimnasio.html` usa llaves de `localStorage` con prefijo `gym_` (`gym_level`, `gym_completed`, `gym_current`), completamente independientes de las de Casa.
- `gimnasio.html` NO incluye el botón de sync (`sync.js` no se carga en esa página) — su progreso se queda solo local en esta spec.
- Mismos 5 grupos musculares (`MUSCLE_CYCLE`) y mismos 4 niveles (`LEVELS`) para ambos modos — viven una sola vez dentro de `plan-engine.js`, no se duplican.
- Spec completo: `docs/superpowers/specs/2026-08-21-gimnasio-design.md`.

---

### Task 1: Crear `plan-engine.js`

**Files:**
- Create: `plan-engine.js`

**Interfaces:**
- Produces (usado por Task 2 y Task 3): `window.PlanEngine.init(config)`, donde:
  ```
  config = {
    equipmentUrls: string[],          // 1+ URLs del CDN a combinar
    keyPrefix: string,                // ej. 'ent_' o 'gym_' — se concatena con 'level'/'completed'/'current'
    excludeExercise?: (exercise) => boolean,  // opcional; si true, el ejercicio no entra al pool
    chairTip?: (exercise) => boolean,         // opcional; marca exercise.chairTip
    onSaveLevel?: (levelKey: string) => void,
    onSaveCompleted?: (completedDays: number[]) => void,
    onSaveCurrentDay?: (day: number) => void,
    pullRemoteState?: () => Promise<{level, currentDay, completedDays}|null>
  }
  ```
  `exercise` trae al menos `{ slug, name, bodyPart, gifUrl }` (shape del CDN, sin cambios).
- Requiere en el HTML que lo carga los mismos IDs de elemento que usa `entrenamientos.html` hoy: `sync-btn` es opcional (el motor nunca lo toca), pero son obligatorios: `level-cancel-wrap`, `level-screen`, `calendar-screen`, `player-screen`, `complete-screen`, `egg-overlay`, `egg-text`, `cal-grid`, `cal-level`, `cal-progress`, `player-head`, `ex-img`, `ex-name`, `ex-reps`, `ex-tip`, `player-controls`, `btn-prev`, `btn-pause`, `btn-done`, `rest-box`, `rest-timer`, `rest-next`, `rest-pause-btn`, `upcoming-wrap`, `upcoming-list`, `complete-day`.

- [ ] **Step 1: Escribir `plan-engine.js`**

```javascript
/* =====================================================================
   plan-engine.js — motor genérico del plan de 30 días (calendario,
   niveles, pantalla de ejercicio, descanso, easter egg espartano).
   Sin conocimiento de Supabase ni de reglas de filtrado de equipo —
   todo eso entra vía PlanEngine.init(config). Compartido por
   entrenamientos.html (Casa) y gimnasio.html (Gimnasio).
   Ver docs/superpowers/specs/2026-08-21-gimnasio-design.md
   ===================================================================== */

/* ---------- lógica pura (testeable sin red/DOM) ---------- */
const LEVELS = {
  facil:     { label:"Fácil",     icon:"🌱", count:4, reps:[8,10,12] },
  medio:     { label:"Medio",     icon:"🔥", count:6, reps:[10,12,15] },
  dificil:   { label:"Difícil",   icon:"💥", count:8, reps:[15,18,20] },
  espartano: { label:"Espartano", icon:"⚔️", count:8, reps:[20,20,20] }
};

const MUSCLE_CYCLE = [
  { key:'legs',  label:'Piernas', icon:'🦵', bodyParts:['legs'] },
  { key:'core',  label:'Core',    icon:'🧘', bodyParts:['core'] },
  { key:'arms',  label:'Brazos',  icon:'💪', bodyParts:['arms','shoulders'] },
  { key:'back',  label:'Espalda', icon:'🏋️', bodyParts:['back'] },
  { key:'chest', label:'Pecho',   icon:'🫁', bodyParts:['chest'] }
];

let POOL = { status:'idle', byGroup:{} };
let LEVEL_KEY = null;

function groupForDay(day){ return MUSCLE_CYCLE[(day-1)%5]; }

function exercisesForDay(day){
  const g = groupForDay(day);
  const pool = POOL.byGroup[g.key] || [];
  if(!pool.length) return [];
  const L = LEVELS[LEVEL_KEY];
  const n = Math.min(L.count, pool.length);
  const cycleIndex = Math.floor((day-1)/5);
  const offset = (cycleIndex*L.count) % pool.length;
  const out = [];
  for(let i=0;i<n;i++) out.push(pool[(offset+i)%pool.length]);
  return out;
}

function repsForDay(day){
  const L = LEVELS[LEVEL_KEY];
  const block = Math.min(2, Math.floor((day-1)/10));
  return L.reps[block];
}
/* ---------- fin lógica pura ---------- */

let CONFIG = null;
let poolPromise = null;
let COMPLETED = new Set();
let CURRENT_DAY_SAVED = null;
let currentDay = null, currentSession = [], currentIdx = 0;
let paused = false, pausedAt = 0, restLeft = 0, restEndAt = 0, restWarned = false, restTimerId = null;
let navEpoch = 0;

/* ---------- persistencia ---------- */
function saveLevel(){
  try{ localStorage.setItem(CONFIG.keyPrefix+'level', LEVEL_KEY); }catch(e){}
  if(CONFIG.onSaveLevel) CONFIG.onSaveLevel(LEVEL_KEY);
}
function loadLevel(){ try{ return localStorage.getItem(CONFIG.keyPrefix+'level'); }catch(e){ return null; } }
function saveCompleted(){
  try{ localStorage.setItem(CONFIG.keyPrefix+'completed', JSON.stringify([...COMPLETED])); }catch(e){}
  if(CONFIG.onSaveCompleted) CONFIG.onSaveCompleted([...COMPLETED]);
}
function loadCompleted(){
  try{ const v=localStorage.getItem(CONFIG.keyPrefix+'completed'); return v ? new Set(JSON.parse(v)) : new Set(); }
  catch(e){ return new Set(); }
}
function saveCurrentDay(day){
  try{ localStorage.setItem(CONFIG.keyPrefix+'current', String(day)); }catch(e){}
  if(CONFIG.onSaveCurrentDay) CONFIG.onSaveCurrentDay(day);
}
function loadCurrentDay(){
  try{ const v=localStorage.getItem(CONFIG.keyPrefix+'current'); return v ? parseInt(v,10) : null; }
  catch(e){ return null; }
}

/* ---------- audio/vibración ---------- */
let actx=null;
function initAudio(){
  if(!actx){ try{ actx=new (window.AudioContext||window.webkitAudioContext)(); }catch(e){} }
  if(actx && actx.state==='suspended') actx.resume();
}
function beep(freq,dur,vol){
  if(!actx) return;
  try{
    const o=actx.createOscillator(), g=actx.createGain();
    o.type='sine'; o.frequency.value=freq;
    g.gain.setValueAtTime(vol||0.25, actx.currentTime);
    g.gain.exponentialRampToValueAtTime(0.001, actx.currentTime+dur);
    o.connect(g); g.connect(actx.destination);
    o.start(); o.stop(actx.currentTime+dur);
  }catch(e){}
}
function vibrate(p){ if(navigator.vibrate) try{ navigator.vibrate(p); }catch(e){} }
document.body.addEventListener('touchstart', initAudio, {once:true});
document.body.addEventListener('click', initAudio, {once:true});

/* ---------- datos ---------- */
async function ensurePool(){
  if(POOL.status==='ready') return true;
  if(poolPromise) return poolPromise;
  POOL = { status:'loading', byGroup:{} };
  poolPromise = (async()=>{
    try{
      const responses = await Promise.all(CONFIG.equipmentUrls.map(u=>fetch(u)));
      responses.forEach(res=>{ if(!res.ok) throw new Error('HTTP '+res.status); });
      const datas = await Promise.all(responses.map(r=>r.json()));
      const byGroup = {};
      MUSCLE_CYCLE.forEach(g=>{ byGroup[g.key]=[]; });
      datas.forEach(data=>{
        data.exercises.forEach(e=>{
          if(CONFIG.excludeExercise && CONFIG.excludeExercise(e)) return;
          if(CONFIG.chairTip) e.chairTip = CONFIG.chairTip(e);
          const g = MUSCLE_CYCLE.find(g=>g.bodyParts.includes(e.bodyPart));
          if(g) byGroup[g.key].push(e);
        });
      });
      MUSCLE_CYCLE.forEach(g=>{ byGroup[g.key].sort((a,b)=>a.slug.localeCompare(b.slug)); });
      POOL = { status:'ready', byGroup };
      return true;
    }catch(e){
      POOL = { status:'error', byGroup:{} };
      return false;
    }finally{
      poolPromise = null;
    }
  })();
  return poolPromise;
}

/* ---------- pantallas ---------- */
function showScreen(name){
  navEpoch++;
  document.querySelectorAll('.screen').forEach(s=>s.classList.remove('show'));
  document.getElementById(name+'-screen').classList.add('show');
}

function openLevelPicker(){
  stopRestTimer();
  document.getElementById('level-cancel-wrap').style.display = LEVEL_KEY ? '' : 'none';
  showScreen('level');
}

function pickLevel(k){
  LEVEL_KEY = k; saveLevel();
  renderCalendar();
  showScreen('calendar');
}

/* ---------- calendario ---------- */
function renderCalendar(){
  const grid = document.getElementById('cal-grid');
  grid.innerHTML = '';
  for(let d=1; d<=30; d++){
    const g = groupForDay(d);
    let cls = 'cal-day';
    if(COMPLETED.has(d)) cls += ' done';
    if(d===CURRENT_DAY_SAVED) cls += ' current';
    const btn = document.createElement('button');
    btn.className = cls;
    btn.innerHTML = `<span class="cal-num">${d}</span><span class="cal-icon">${g.icon}</span>`;
    btn.onclick = ()=>openDayFlow(d);
    grid.appendChild(btn);
  }
  document.getElementById('cal-progress').textContent = `${COMPLETED.size}/30 completados`;
  document.getElementById('cal-level').textContent = `${LEVELS[LEVEL_KEY].icon} ${LEVELS[LEVEL_KEY].label}`;
}

function backToCalendar(){
  stopRestTimer();
  renderCalendar();
  showScreen('calendar');
}

/* ---------- abrir un día ---------- */
async function openDayFlow(d){
  if(POOL.status!=='ready'){
    showScreen('player');
    const myEpoch = navEpoch;
    document.getElementById('player-head').textContent = 'Cargando ejercicios…';
    document.getElementById('ex-img').style.display = 'none';
    document.getElementById('ex-name').textContent = '';
    document.getElementById('ex-reps').textContent = '';
    document.getElementById('player-controls').style.display = 'none';
    document.getElementById('rest-box').classList.remove('show');
    document.getElementById('upcoming-wrap').style.display = 'none';
    const ok = await ensurePool();
    if(myEpoch !== navEpoch) return; // el usuario ya navegó a otra pantalla mientras esperábamos
    if(!ok){
      document.getElementById('player-head').textContent = 'No se pudo cargar (revisa tu internet).';
      return;
    }
  }
  openDay(d);
}

function openDay(day){
  stopRestTimer();
  currentDay = day;
  currentSession = exercisesForDay(day);
  currentIdx = 0;
  CURRENT_DAY_SAVED = day;
  saveCurrentDay(day);
  document.getElementById('upcoming-wrap').style.display = '';
  document.getElementById('player-controls').style.display = 'flex';
  showScreen('player');
  maybeShowEgg(day, renderExercise);
}

/* ---------- easter egg espartano ---------- */
function maybeShowEgg(day, cb){
  if(LEVEL_KEY==='espartano' && day>=21 && day<=30){
    const el = document.getElementById('egg-overlay');
    const txt = document.getElementById('egg-text');
    el.classList.add('show');
    txt.textContent = 'Esparta, mi Esparta famosa por sus hombres, es mi patria.';
    setTimeout(()=>{
      txt.textContent = 'LISTO ESPARTANO';
      setTimeout(()=>{ el.classList.remove('show'); cb(); }, 4000);
    }, 2000);
  } else cb();
}

/* ---------- pantalla de ejercicio ---------- */
function renderExercise(){
  paused = false;
  document.getElementById('rest-box').classList.remove('show');
  document.getElementById('player-controls').style.display = 'flex';
  document.getElementById('upcoming-wrap').style.display = '';

  const g = groupForDay(currentDay);
  document.getElementById('player-head').textContent =
    `Día ${currentDay} · ${g.icon} ${g.label} · ${currentIdx+1}/${currentSession.length}`;

  const ex = currentSession[currentIdx];
  const img = document.getElementById('ex-img');
  const tip = document.getElementById('ex-tip');
  if(ex){
    img.removeAttribute('src');
    img.src = ex.gifUrl; img.alt = ex.name; img.style.display = '';
    document.getElementById('ex-name').textContent = ex.name;
    document.getElementById('ex-reps').textContent = `x ${repsForDay(currentDay)}`;
    if(ex.chairTip){ tip.textContent = '💡 ¿No tienes banco? Usa una silla.'; tip.classList.add('show'); }
    else tip.classList.remove('show');
  } else {
    img.removeAttribute('src'); img.style.display = 'none';
    document.getElementById('ex-name').textContent = 'Sin ejercicios disponibles.';
    document.getElementById('ex-reps').textContent = '';
    tip.classList.remove('show');
  }
  syncPauseUI();
  renderUpcoming();
}

function renderUpcoming(){
  const list = document.getElementById('upcoming-list');
  list.innerHTML = '';
  const rest = currentSession.slice(currentIdx+1);
  rest.forEach(ex=>{
    const row = document.createElement('div');
    row.className = 'up-row';
    row.textContent = ex.name;
    list.appendChild(row);
  });
  document.getElementById('upcoming-wrap').style.display = rest.length ? '' : 'none';
}

function prevExercise(){
  if(paused || currentIdx<=0) return;
  currentIdx--;
  renderExercise();
}

function togglePause(){
  paused = !paused;
  if(paused){ pausedAt = Date.now(); }
  else if(pausedAt){ restEndAt += (Date.now()-pausedAt); pausedAt = 0; }
  syncPauseUI();
}

function syncPauseUI(){
  const label = paused ? '▶' : '⏸';
  const btnEx = document.getElementById('btn-pause'); if(btnEx) btnEx.textContent = label;
  const btnRest = document.getElementById('rest-pause-btn'); if(btnRest) btnRest.textContent = label;
  document.getElementById('btn-done').disabled = paused || !currentSession.length;
  document.getElementById('btn-prev').disabled = paused || currentIdx===0;
}

/* ---------- marcar hecho / descanso ---------- */
function markDone(){
  initAudio();
  if(paused || !currentSession.length) return;
  if(currentIdx >= currentSession.length-1){
    completeSession();
    return;
  }
  startRest();
}

function stopRestTimer(){
  clearInterval(restTimerId);
  restTimerId = null;
  paused = false;
  pausedAt = 0;
}

function startRest(){
  stopRestTimer();
  restEndAt = Date.now() + 30000;
  restWarned = false;
  document.getElementById('player-controls').style.display = 'none';
  const box = document.getElementById('rest-box');
  box.classList.add('show');
  restLeft = 30;
  updateRestUI();
  restTimerId = setInterval(tickRest, 250);
}

function tickRest(){
  if(paused) return;
  restLeft = Math.max(0, Math.ceil((restEndAt-Date.now())/1000));
  if(!restWarned && restLeft<=10 && restLeft>0){
    restWarned = true; vibrate([90,70,90]); beep(1320,0.1,0.28);
  }
  if(restLeft<=0){
    stopRestTimer();
    currentIdx++;
    renderExercise();
    return;
  }
  updateRestUI();
}

function updateRestUI(){
  document.getElementById('rest-timer').textContent = restLeft;
  const next = currentSession[currentIdx+1];
  document.getElementById('rest-next').textContent = next ? `Siguiente: ${next.name}` : 'Último ejercicio';
  syncPauseUI();
}

function completeSession(){
  COMPLETED.add(currentDay);
  saveCompleted();
  document.getElementById('complete-day').textContent = currentDay;
  showScreen('complete');
}

/* ---------- init ---------- */
async function init(config){
  CONFIG = Object.assign({
    keyPrefix:'', equipmentUrls:[], excludeExercise:null, chairTip:null,
    onSaveLevel:null, onSaveCompleted:null, onSaveCurrentDay:null, pullRemoteState:null
  }, config);

  LEVEL_KEY = loadLevel();
  COMPLETED = loadCompleted();
  CURRENT_DAY_SAVED = loadCurrentDay();

  if(CONFIG.pullRemoteState){
    const remote = await CONFIG.pullRemoteState();
    if(remote){
      if(remote.level){ LEVEL_KEY = remote.level; try{ localStorage.setItem(CONFIG.keyPrefix+'level', LEVEL_KEY); }catch(e){} }
      if(remote.currentDay != null){ CURRENT_DAY_SAVED = remote.currentDay; try{ localStorage.setItem(CONFIG.keyPrefix+'current', String(remote.currentDay)); }catch(e){} }
      if(remote.completedDays && remote.completedDays.length){
        COMPLETED = new Set(remote.completedDays);
        try{ localStorage.setItem(CONFIG.keyPrefix+'completed', JSON.stringify([...COMPLETED])); }catch(e){}
      }
    }
  }

  if(LEVEL_KEY && LEVELS[LEVEL_KEY]){
    renderCalendar();
    showScreen('calendar');
  } else {
    LEVEL_KEY = null;
    showScreen('level');
  }
  ensurePool();
}

window.PlanEngine = { init };
```

- [ ] **Step 2: Verificar sintaxis**

```bash
node --check plan-engine.js
```
Expected: sin salida (exit code 0).

- [ ] **Step 3: Assert-based check de la lógica pura**

Mismo patrón que `docs/superpowers/plans/2026-08-20-modo-casa.md` y `sync.js`: extraer el bloque marcado, agregarle el test, correrlo en un archivo temporal.

```bash
node -e "
const fs=require('fs');
const src=fs.readFileSync('plan-engine.js','utf8');
const start=src.indexOf('/* ---------- lógica pura');
const end=src.indexOf('/* ---------- fin lógica pura ---------- */');
const pureCode=src.slice(start,end);
const test=pureCode+\`
// día 1 → legs, día 6 → legs otra vez (ciclo de 5), día 3 → arms (combina arms+shoulders)
console.assert(groupForDay(1).key==='legs', 'día 1 debe ser legs');
console.assert(groupForDay(6).key==='legs', 'día 6 debe volver a legs (ciclo de 5)');
console.assert(groupForDay(3).key==='arms', 'día 3 debe ser arms');

POOL = { status:'ready', byGroup: { legs: [{slug:'a'},{slug:'b'},{slug:'c'},{slug:'d'},{slug:'e'}] } };
LEVEL_KEY = 'facil'; // count:4
const day1 = exercisesForDay(1);
console.assert(day1.length===4, 'nivel facil debe dar 4 ejercicios, dio '+day1.length);
const day6 = exercisesForDay(6); // mismo grupo (legs), siguiente ciclo -> offset distinto
console.assert(day6.length===4, 'día 6 (mismo grupo) también debe dar 4 ejercicios');
console.assert(JSON.stringify(day1)!==JSON.stringify(day6), 'el ciclo 2 debe rotar el offset, no repetir exactamente el mismo subconjunto');

console.assert(repsForDay(1)===8, 'facil día 1 (bloque 1-10) debe ser 8 reps, dio '+repsForDay(1));
console.assert(repsForDay(11)===10, 'facil día 11 (bloque 11-20) debe ser 10 reps, dio '+repsForDay(11));
console.assert(repsForDay(25)===12, 'facil día 25 (bloque 21-30) debe ser 12 reps, dio '+repsForDay(25));

console.log('OK plan-engine pure logic');
\`;
fs.writeFileSync('.plan-engine-check.js', test);
"
node .plan-engine-check.js
rm -f .plan-engine-check.js
```
Expected output: `OK plan-engine pure logic` y ninguna línea `Assertion failed`.

- [ ] **Step 4: Commit**

```bash
git add plan-engine.js
git commit -m "feat(gimnasio): add plan-engine.js — generic 30-day plan engine extracted for reuse"
```

---

### Task 2: Refactorizar `entrenamientos.html` para usar `plan-engine.js`

**Files:**
- Modify: `entrenamientos.html`

**Interfaces:**
- Consumes: `window.PlanEngine.init(config)` de Task 1.

- [ ] **Step 1: Reemplazar el bloque de script completo**

Ubicar todo el contenido desde `<script src="sync.js"></script>` (línea 184 antes de este cambio) hasta el `</script>` que le sigue justo antes de `</body>` (línea 573), e.d. **todo** el bloque:

```html
<script src="sync.js"></script>
<script>
/* =====================================================================
   ENTRENAMIENTOS — plan de 30 días, peso corporal, sin equipo
   ...
   ===================================================================== */
[... todo el JS actual, líneas 186-572 ...]
</script>
```

reemplazar por:

```html
<script src="sync.js"></script>
<script src="plan-engine.js"></script>
<script>
/* =====================================================================
   ENTRENAMIENTOS — shell de Casa sobre plan-engine.js: define de dónde
   salen los ejercicios (bodyweight), qué se excluye por falta de
   equipo, y cómo se conecta con sync.js. El motor del plan vive en
   plan-engine.js, compartido con gimnasio.html.
   ===================================================================== */

/* Filtra ejercicios que piden equipo poco común en casa (pelotas/fitball/bosu, barras/barras
   paralelas, dominadas/muscle-ups/colgado que necesitan barra aunque el slug no diga "bar") —
   no toda la gente lo tiene. Los de banco se dejan pero se marcan para sugerir una silla en su
   lugar. Basado en los slugs/nombres reales del catálogo, no en suposiciones. */
const NO_EQUIPMENT_RE = /ball|balon|balón|pelota|medicine|swiss|estabilidad|barbell|ez-bar|\bbars?\b|barra|pull-up|pullup|chin-up|chinup|muscle-up|muscleup|hanging|colgad|dominad|dip-cage|jaula de dominadas|captains-chair|silla del capitan|silla del capitán|ring-dips|anillas/i;
const CHAIR_TIP_RE = /bench|banco/i;

(async function(){
  if(window.QuemaSync) await QuemaSync.init(document.getElementById('sync-btn'));
  PlanEngine.init({
    equipmentUrls: ['https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0/api/es/equipment/bodyweight.json'],
    keyPrefix: 'ent_',
    excludeExercise: e => NO_EQUIPMENT_RE.test(e.slug+' '+e.name),
    chairTip: e => CHAIR_TIP_RE.test(e.slug+' '+e.name),
    onSaveLevel: level => { if(window.QuemaSync) QuemaSync.pushUserState({ plan_level: level }); },
    onSaveCompleted: days => { if(window.QuemaSync) QuemaSync.pushUserState({ plan_completed_days: days }); },
    onSaveCurrentDay: day => { if(window.QuemaSync) QuemaSync.pushUserState({ plan_current_day: day }); },
    pullRemoteState: async () => (window.QuemaSync && QuemaSync.isSignedIn()) ? await QuemaSync.pullUserState() : null
  });
})();
</script>
```

No toques nada del HTML/CSS del resto del archivo (topbar, pantallas, `<style>`) — el refactor es solo de este bloque de script.

- [ ] **Step 2: Verificar sintaxis**

```bash
node -e "
const fs=require('fs');
const src=fs.readFileSync('entrenamientos.html','utf8');
const body=src.slice(src.lastIndexOf('<script>')+8, src.lastIndexOf('</script>'));
fs.writeFileSync('.syntax-check.js', body);
"
node --check .syntax-check.js
rm -f .syntax-check.js
```
Expected: sin salida (exit code 0).

- [ ] **Step 3: Regresión manual completa en navegador**

Sirve el sitio localmente (ej. `npx serve .` o cualquier servidor estático — no `file://`, el CSP lo bloquea) y en `entrenamientos.html`:
1. Si ya tenías un nivel guardado de antes, confirma que carga el calendario con tu progreso intacto (mismo nivel, mismos días marcados ✓).
2. Si no, elige un nivel, confirma que el calendario se arma con 30 días.
3. Abre un día, confirma que carga un ejercicio con GIF, nombre y reps correctos para tu nivel/tercio del plan.
4. Marca ✓, confirma que arranca el descanso de 30s con cuenta regresiva y aviso a los 10s (beep/vibración).
5. Deja que el descanso termine solo, confirma que avanza al siguiente ejercicio.
6. Completa el último ejercicio de una sesión, confirma que llega a la pantalla "Día X completo" y el día queda marcado ✓ en el calendario.
7. Recarga la página, confirma que el progreso persiste.
8. Si tienes nivel Espartano y estás en un día 21-30, confirma que el easter egg sigue apareciendo.
9. Si tienes sesión activa de sync (Tarea 5 del proyecto de Supabase), confirma que el botón ☁️ sigue funcionando igual que antes del refactor.

Si algo de esto se comporta distinto a como se comportaba antes del refactor, es un bug de la extracción — no continúes a Task 3 hasta que quede idéntico.

- [ ] **Step 4: Commit**

```bash
git add entrenamientos.html
git commit -m "refactor(entrenamientos): move plan engine to plan-engine.js, keep Casa-specific config as a thin shell"
```

---

### Task 3: Crear `gimnasio.html`

**Files:**
- Create: `gimnasio.html`

**Interfaces:**
- Consumes: `window.PlanEngine.init(config)` de Task 1. No consume `sync.js`.

- [ ] **Step 1: Escribir `gimnasio.html`**

Mismo HTML/CSS que `entrenamientos.html` (topbar sin botón de sync, mismo `<style>`, mismas pantallas/IDs), con su propio título, CSP (idéntico al de `entrenamientos.html` salvo que no necesita `https://esm.sh` ni `https://*.supabase.co` en el CSP, porque esta página no carga `sync.js`) y bloque de inicialización:

```html
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="Gimnasio">
<meta name="mobile-web-app-capable" content="yes">
<meta name="theme-color" content="#0d0d0f">
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src https://fonts.gstatic.com; script-src 'self' 'unsafe-inline'; img-src 'self' data: https://cdn.jsdelivr.net; connect-src https://cdn.jsdelivr.net; frame-ancestors 'none'; base-uri 'none'; form-action 'none';">
<title>Gimnasio · Plan 30 días</title>
<link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Syne:wght@400;700;800&display=swap" rel="stylesheet">
<style>
*, *::before, *::after { box-sizing:border-box; margin:0; padding:0; }
:root {
  --bg:#0d0d0f; --surface:#18181c; --surface2:#222228;
  --border:rgba(255,255,255,0.08); --text:#f0efe8; --muted:#777;
  --accent:#EF9F27; --accent-text:#000;
  --safe-top: env(safe-area-inset-top, 0px);
  --safe-bot: env(safe-area-inset-bottom, 0px);
}
html, body { height:100%; background:var(--bg); overscroll-behavior:none; }
body { color:var(--text); font-family:'Syne',sans-serif; padding-top:var(--safe-top);
  -webkit-user-select:none; user-select:none; touch-action:manipulation; }

.topbar { display:flex; align-items:center; justify-content:space-between;
  padding:12px 14px 9px 18px; border-bottom:1px solid var(--border); }
.topbar-title { font-size:13px; font-weight:800; letter-spacing:.03em; }
.gear-btn { background:var(--surface2); border:1px solid var(--border); color:var(--text);
  width:34px; height:34px; border-radius:10px; font-size:15px; cursor:pointer;
  display:flex; align-items:center; justify-content:center; text-decoration:none;
  -webkit-tap-highlight-color:transparent; }

.screen { display:none; padding:20px calc(20px + env(safe-area-inset-right,0px)) calc(24px + var(--safe-bot));
  max-width:480px; margin:0 auto; }
.screen.show { display:block; }

/* ---- nivel ---- */
.level-intro { font-size:13px; color:var(--muted); margin-bottom:16px; line-height:1.5; }
.level-grid { display:grid; grid-template-columns:1fr 1fr; gap:10px; }
.level-card { width:100%; background:var(--surface); border:1.5px solid var(--border); border-radius:16px;
  padding:18px 10px; text-align:center; cursor:pointer; color:var(--text); font-family:'Syne',sans-serif;
  -webkit-tap-highlight-color:transparent; }
.level-card .l-icon { font-size:26px; display:block; margin-bottom:6px; }
.level-card .l-name { font-weight:800; font-size:14px; display:block; }
.level-card .l-sub { font-size:10px; color:var(--muted); margin-top:3px; display:block; }

/* ---- calendario ---- */
.cal-head { display:flex; justify-content:space-between; align-items:center; margin-bottom:14px; font-size:12px; color:var(--muted); }
.cal-grid { display:grid; grid-template-columns:repeat(5,1fr); gap:8px; }
.cal-day { background:var(--surface); border:1.5px solid var(--border); border-radius:12px;
  padding:10px 2px; text-align:center; cursor:pointer; color:var(--text); font-family:'Space Mono',monospace;
  -webkit-tap-highlight-color:transparent; }
.cal-day.done { background:var(--accent); border-color:var(--accent); color:var(--accent-text); }
.cal-day.current:not(.done) { border-color:var(--accent); border-width:2px; }
.cal-num { display:block; font-weight:700; font-size:15px; }
.cal-icon { display:block; font-size:12px; margin-top:2px; }

/* ---- jugador de ejercicio ---- */
.player-back { background:none; border:none; color:var(--muted); font-family:'Syne',sans-serif;
  font-size:12px; cursor:pointer; padding:0 0 14px; -webkit-tap-highlight-color:transparent; }
.player-head { font-size:11px; color:var(--muted); text-align:center; margin-bottom:12px;
  letter-spacing:.05em; text-transform:uppercase; }
.ex-gif { width:100%; max-width:280px; display:block; margin:0 auto; border-radius:16px; background:var(--surface2); }
.ex-name { text-align:center; font-size:20px; font-weight:800; margin-top:14px; }
.ex-reps { text-align:center; font-family:'Space Mono',monospace; font-size:34px; font-weight:700; color:var(--accent); margin-top:6px; }
.ex-tip { display:none; text-align:center; font-size:11.5px; color:var(--muted); background:var(--surface);
  border:1px solid var(--border); border-radius:10px; padding:7px 12px; margin:10px auto 0; max-width:280px; }
.ex-tip.show { display:block; }
.player-controls { display:flex; gap:8px; margin-top:18px; }
.p-btn { border:none; border-radius:14px; padding:14px; font-family:'Syne',sans-serif;
  font-size:13px; font-weight:700; cursor:pointer; -webkit-tap-highlight-color:transparent; }
.p-btn-done { background:var(--accent); color:var(--accent-text); flex:2; }
.p-btn-prev, .p-btn-pause { background:var(--surface2); color:var(--text); border:1px solid var(--border); flex:1; }
.p-btn:disabled { opacity:.35; cursor:default; }

.rest-box { display:none; text-align:center; margin-top:18px; }
.rest-box.show { display:block; }
.rest-timer { font-family:'Space Mono',monospace; font-size:56px; font-weight:700; color:var(--accent); }
.rest-next { font-size:13px; color:var(--muted); margin-top:6px; }
.rest-pause { margin-top:14px; background:var(--surface2); border:1px solid var(--border); color:var(--text);
  border-radius:12px; padding:10px 22px; cursor:pointer; font-family:'Syne',sans-serif; font-size:13px;
  -webkit-tap-highlight-color:transparent; }

.upcoming-label { font-size:10px; color:var(--muted); text-transform:uppercase; letter-spacing:.08em; margin:22px 0 8px; }
.up-row { padding:8px 0; border-bottom:1px solid var(--border); font-size:13px; }

/* ---- sesión completa ---- */
.complete-box { text-align:center; padding-top:60px; }
.complete-icon { font-size:60px; }
.complete-title { font-size:24px; font-weight:800; margin-top:10px; }
.complete-sub { color:var(--muted); margin-top:8px; font-size:13px; }
.complete-btn { margin-top:26px; background:var(--accent); color:var(--accent-text); border:none;
  border-radius:14px; padding:14px 30px; font-weight:700; font-family:'Syne',sans-serif; cursor:pointer;
  -webkit-tap-highlight-color:transparent; }

/* ---- easter egg espartano ---- */
.egg-overlay { position:fixed; inset:0; background:#0d0d0f; display:flex; align-items:center; justify-content:center;
  z-index:300; opacity:0; pointer-events:none; transition:opacity .3s; padding:30px; text-align:center; }
.egg-overlay.show { opacity:1; pointer-events:all; }
.egg-text { font-size:19px; font-weight:800; color:var(--accent); line-height:1.5; }
</style>
</head>
<body>

<div class="topbar">
  <a class="gear-btn" href="index.html" title="Volver al inicio">←</a>
  <span class="topbar-title">🏢 Gimnasio</span>
  <button class="gear-btn" id="level-gear-btn" onclick="openLevelPicker()">⚙</button>
</div>

<div class="screen" id="level-screen">
  <div id="level-cancel-wrap" style="display:none;margin-bottom:14px;">
    <button class="gear-btn" style="width:auto;padding:0 14px;" onclick="backToCalendar()">← Cancelar</button>
  </div>
  <p class="level-intro">Elige tu nivel para empezar el plan. Define cuántos ejercicios haces por sesión — puedes cambiarlo después.</p>
  <div class="level-grid">
    <button class="level-card" onclick="pickLevel('facil')">
      <span class="l-icon">🌱</span><span class="l-name">Fácil</span><span class="l-sub">4 ejercicios</span>
    </button>
    <button class="level-card" onclick="pickLevel('medio')">
      <span class="l-icon">🔥</span><span class="l-name">Medio</span><span class="l-sub">6 ejercicios</span>
    </button>
    <button class="level-card" onclick="pickLevel('dificil')">
      <span class="l-icon">💥</span><span class="l-name">Difícil</span><span class="l-sub">8 ejercicios</span>
    </button>
    <button class="level-card" onclick="pickLevel('espartano')">
      <span class="l-icon">⚔️</span><span class="l-name">Espartano</span><span class="l-sub">8, más intenso</span>
    </button>
  </div>
</div>

<div class="screen" id="calendar-screen">
  <div class="cal-head">
    <span id="cal-level"></span>
    <span id="cal-progress"></span>
  </div>
  <div class="cal-grid" id="cal-grid"></div>
</div>

<div class="screen" id="player-screen">
  <button class="player-back" onclick="backToCalendar()">← Calendario</button>
  <div class="player-head" id="player-head"></div>
  <img class="ex-gif" id="ex-img" alt="">
  <div class="ex-name" id="ex-name"></div>
  <div class="ex-reps" id="ex-reps"></div>
  <div class="ex-tip" id="ex-tip"></div>

  <div class="player-controls" id="player-controls">
    <button class="p-btn p-btn-prev" id="btn-prev" onclick="prevExercise()">⏮</button>
    <button class="p-btn p-btn-pause" id="btn-pause" onclick="togglePause()">⏸</button>
    <button class="p-btn p-btn-done" id="btn-done" onclick="markDone()">✓ Listo</button>
  </div>

  <div class="rest-box" id="rest-box">
    <div class="rest-timer" id="rest-timer">30</div>
    <div class="rest-next" id="rest-next"></div>
    <button class="rest-pause" id="rest-pause-btn" onclick="togglePause()">⏸</button>
  </div>

  <div id="upcoming-wrap">
    <div class="upcoming-label">Siguen</div>
    <div id="upcoming-list"></div>
  </div>
</div>

<div class="screen" id="complete-screen">
  <div class="complete-box">
    <div class="complete-icon">🔥</div>
    <div class="complete-title">¡Día <span id="complete-day"></span> completo!</div>
    <div class="complete-sub">Sigue así — te espera el siguiente día en el calendario.</div>
    <button class="complete-btn" onclick="backToCalendar()">Volver al calendario</button>
  </div>
</div>

<div class="egg-overlay" id="egg-overlay">
  <div class="egg-text" id="egg-text"></div>
</div>

<script src="plan-engine.js"></script>
<script>
/* Gimnasio: sin filtro de equipo (todo el pool requiere equipo, ese es el punto),
   sin chairTip, sin sync — progreso 100% local con prefijo gym_. */
PlanEngine.init({
  equipmentUrls: [
    'https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0/api/es/equipment/band.json',
    'https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0/api/es/equipment/barbell.json',
    'https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0/api/es/equipment/cable.json',
    'https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0/api/es/equipment/dumbbell.json',
    'https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0/api/es/equipment/ez-bar.json',
    'https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0/api/es/equipment/kettlebell.json',
    'https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0/api/es/equipment/lever.json',
    'https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0/api/es/equipment/machine.json',
    'https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0/api/es/equipment/other.json',
    'https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0/api/es/equipment/sled.json',
    'https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0/api/es/equipment/smith.json'
  ],
  keyPrefix: 'gym_'
});
</script>
</body>
</html>
```

- [ ] **Step 2: Verificar sintaxis**

```bash
node -e "
const fs=require('fs');
const src=fs.readFileSync('gimnasio.html','utf8');
const body=src.slice(src.lastIndexOf('<script>')+8, src.lastIndexOf('</script>'));
fs.writeFileSync('.syntax-check.js', body);
"
node --check .syntax-check.js
rm -f .syntax-check.js
```
Expected: sin salida (exit code 0).

- [ ] **Step 3: Commit**

```bash
git add gimnasio.html
git commit -m "feat(gimnasio): add gimnasio.html — 30-day gym plan on plan-engine.js"
```

---

### Task 4: Enlazar Gimnasio desde `index.html`

**Files:**
- Modify: `index.html`

- [ ] **Step 1: Agregar la tarjeta de Gimnasio**

Ubicar:

```html
    <a class="mode-row" href="entrenamientos.html">
      <span class="mode-icon">🏠</span>
      <span class="mode-text"><span class="mode-name">Casa</span><span class="mode-sub">Plan de 30 días</span></span>
      <span class="mode-chevron">›</span>
    </a>
    <a class="mode-row accent" href="rapido.html">
```

reemplazar por:

```html
    <a class="mode-row" href="entrenamientos.html">
      <span class="mode-icon">🏠</span>
      <span class="mode-text"><span class="mode-name">Casa</span><span class="mode-sub">Plan de 30 días</span></span>
      <span class="mode-chevron">›</span>
    </a>
    <a class="mode-row" href="gimnasio.html">
      <span class="mode-icon">🏢</span>
      <span class="mode-text"><span class="mode-name">Gimnasio</span><span class="mode-sub">Plan de 30 días · con equipo</span></span>
      <span class="mode-chevron">›</span>
    </a>
    <a class="mode-row accent" href="rapido.html">
```

- [ ] **Step 2: Verificar sintaxis**

```bash
node -e "
const fs=require('fs');
const src=fs.readFileSync('index.html','utf8');
const body=src.slice(src.lastIndexOf('<script>')+8, src.lastIndexOf('</script>'));
fs.writeFileSync('.syntax-check.js', body);
"
node --check .syntax-check.js
rm -f .syntax-check.js
```
Expected: sin salida (exit code 0).

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "feat(gimnasio): link Gimnasio card from the home screen"
```

---

### Task 5: Verificación end-to-end y push

**Files:** ninguno (solo verificación manual + push).

- [ ] **Step 1: Prueba manual completa de Gimnasio en navegador**

Sirve el sitio localmente y en `gimnasio.html`:
1. Elige un nivel, confirma que el calendario se arma con 30 días.
2. Abre el día 1 (Piernas): confirma que carga un ejercicio con GIF de un ejercicio de gimnasio (con mancuerna/máquina/cable visible en la imagen, no de peso corporal) y reps correctas.
3. Completa el flujo de un día entero: marcar ✓ en cada ejercicio, ver el descanso de 30s, llegar a "Día 1 completo".
4. Recarga la página, confirma que el progreso (`gym_*`) persiste y es independiente del de Casa (abre `entrenamientos.html` en otra pestaña y confirma que su calendario no cambió).
5. Confirma que Gimnasio **no** muestra ningún botón de sync (no hay `sync-btn` en su topbar).
6. Si tienes nivel Espartano y estás en un día 21-30, confirma que el easter egg también aparece aquí (mismo motor).

- [ ] **Step 2: Confirmar Casa sigue intacta**

Repite un flujo corto en `entrenamientos.html` (abrir un día, marcar ✓ un ejercicio) para confirmar que el refactor de Task 2 no rompió nada que no se haya visto ya en la regresión de esa tarea.

- [ ] **Step 3: Push y confirmar en vivo**

```bash
git push
```

Espera ~1 minuto y repite la prueba manual del Step 1 contra la URL real de GitHub Pages (`https://pacoalvarezvivaldo-collab.github.io/quemagrasa/gimnasio.html`) para confirmar que el CSP en producción no bloquea nada (mismo tipo de sorpresa que ya pasó con `connect-src`/`script-src` en el proyecto de sync — mejor confirmarlo aquí que asumir que local y producción se comportan igual).
